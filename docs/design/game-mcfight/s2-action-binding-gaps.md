# 最小动作绑定：具体素材缺口

2026-09-14 程序实查。当前 V02 几何样例不计动作验收。

| 样例 | 已有文件与核对结果 | 接入前所缺 |
|---|---|---|
| 共用近战 A | 原目录 Assets/Sprites/Monsters/skeleton/attack.png，实看为持弓单姿态 | 不适合作为挥砍动作；需策划指定近战 A 的动作序列或明确可接受的姿态驱动方案 |
| 共用近战 B | Assets/Sprites/Monsters/vindicator/attack.png，实看为持斧单姿态 | 前摇/挥出/后摇序列、帧时间、手部/武器挂点；不能把单张姿态称为完整挥砍 |
| 俯冲 | Assets/Sprites/Monsters/vex/ 仅找到 idle.png | 指定俯冲单位；下降、接触、回升的动作或姿态映射、打断后的恢复表现 |

上述路径相对 C:/Users/24652/Desktop/projects/apollpgame/mcfight-unity-main/。完整库存见 inventory/assets.md。slash_spritesheet.png 属于攻击特效候选，不能替代两个单位自身的可区分动作。

策划待确认：所选单位和素材文件、朝向/镜像方式、脚底与攻击挂点、允许的命中时点与范围偏差、硬控/死亡时如何结束表现。程序收到后只绑定最小三样例，复用真实 Flow 与 Shape，不新增单位专属执行逻辑。

验收逐项：启动→前摇→生效→后摇的 Tick；动作命中帧对齐实际接触扣血拍；挂点随宿主位置/朝向；逻辑范围叠图；硬控/死亡后不继续播放攻击特效；两近战配置分别保留伤害 7/13、不同距离和半径的实验对照。

远程 V02 仅证明远距离选择并生成定点伤害区。后续最小弹丸另验：从发射挂点生成，Velocity/Motion 真实飞行，经 overlap/trigger/hitbox 接触扣血，命中或寿命到期清理；不得瞬间放到目标处冒充弹丸。本批没有宣称此能力通过。

接续顺序：V02 独立复查 → 上述动作绑定（缺素材时等待策划明确）→ V06 接敌避让 → V07 多段效果 → V08 seed/输入重复及规模耗时。B 已通过结论保持，S2 未完成。

## 2026-09-14 策划确认与绑定字段落地

已确定：卫道士斧劈、僵尸徒手横扫共用近战模板；恼鬼使用俯冲模板。骷髅留给远程。无需再次确认选角。

绑定定义：games/game-mcfight/s2-action-bindings.ts。每套记录统一画布、透明背景、默认朝右、根挂点/身体/握持/效果起点（恼鬼为身体根和接触点）、动作文件/帧数/参考帧率。尚未制作的字段为 null/0，入口显示逐项缺失，绝不自动把单姿态认作完成。

卫道士 idle/axe-windup/axe-release/axe-recovery；僵尸 idle/arm-windup/sweep-release/arm-recovery；恼鬼 hover/prepare/dive/contact/rise/hover-recovery。制作需保持画布、比例和锚点，完整武器轨迹不可裁边；不得拉长现有姿态冒充动作。

合同已记录：等比缩放0.8—1.2、速度0.75—1.5；挂点误差≤1素材像素；调试轮廓直接用Shape且误差0；有效斩击边界≤5%，光晕拖尾不算；事件后首渲染帧显示命中/窗口/取消，不允许拖延一个Tick。前摇锁朝向，目标离远时攻击者原地完成，目标处反馈，不延伸武器。

本次实现的是绑定数据、缺失提示与共享镜像坐标换算；没有声称已完成动作播放器、事件反馈接线或素材品质验收。15项针对回归通过（2项绑定、7项V02、6项旧画面夹具），exit0。镜像测试只证明数学坐标换算，不代替实际图像≤1像素验收。

具体待交付素材：上述14个动作片段的实际文件、固定画布尺寸、各挂点坐标、每片段帧序与参考帧率。它们现在是制作待交付项，不再是策划要求不明确。制作后还需透明边缘、比例、握持关系和逐帧锚点检查。

后续播放器接线须先核对现有呈现能力，不能让动画写HP或窗口；新双技能模板的取消与移动宿主同步仍单列验证。B和V02此前已通过范围保持。
