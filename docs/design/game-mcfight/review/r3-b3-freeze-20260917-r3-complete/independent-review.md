# R3-B3 r3 独立复查报告

日期：2026-09-17  
复查对象：`r3-b3-freeze-20260917-r3-complete/frozen-source`  
结论：**PASS（R3-B3 独立复查通过）**

## 范围与完整性

复查仅在冻结副本内执行，未修改共享施工目录或矩阵。执行前与六组撤修恢复后均核对 `integrity.sha256`，结果均为通过。构建命令产生的 `dist` 输出属于门所要求的生成物；经哈希覆盖的源、测试、配置、证据与截图均保持一致。

## 独立复跑

| 门 | 结果 |
|---|---|
| B3 定向：能力边界、生产身份、可视化夹具 | 3 文件，20 项通过 |
| MC Fight 全量 | 48 文件通过；命令退出码 0。保留 1 项历史跳过。 |
| TypeScript | `npx tsc --noEmit` 退出码 0，标准输出与错误输出均为空。 |
| 构建 | `npm run build` 退出码 0；仅有既有 chunk-size 警告。 |
| R3 组合审计 | 4 图，均 `cycles=0`、`unknown=0`。 |

## 八单位身份核对

生产身份测试覆盖并通过下列八个单位：

- 大象、米诺菇：真实远距冲锋、首次接触伤害与参数差异。
- 恼鬼：生产绑定俯冲窗口且无商店入口。
- 瞻远者：3 次最大生命比例射线后 5 次俯冲的私有成功循环。
- 沙漠蛛蜂：仅节肢命中禁足，禁足不成为硬控。
- 飞行娜迦：毒弹/俯冲交替、真实中毒与无伤害环绕。
- 诡异蚊鬼：同一 body 的一次性原位变身与变身后投射物。
- Teleto：真实标准投射物命中地面目标。

## 撤修证明

执行 `scripts/mcfight-r3-b3-mutation-check.mjs`。以下六项均以退出码 1 验红，脚本随后恢复原始内容：

1. 失败启动也推进循环。
2. 禁足被错误地视为硬控。
3. 变身丢失原 body。
4. 目标消失后仍遗留环绕速度。
5. 中毒丢失来源快照。
6. 中毒丢失周期伤害。

恢复后冻结包哈希再次通过。

## 可视化证据

已核验冻结包中的三张截图及对应生产夹具：

- `01-charge.png`：B3 大象冲锋。
- `02-dive-window.png`：B3 恼鬼俯冲窗口。
- `03-transform.png`：B3 诡异蚊鬼变身。

三者均由 `createR3IdentityScene` 生产 World 读取，夹具测试通过。画面仍为机制观察用几何占位，正式动作与特效属于 S6，不构成 R3-B3 的阻塞项。

## 签核边界

本结论仅签核 R3-B3 八单位的逻辑、生产链路、调度、可重复验证与机制观察证据。它不签核 S6 的真实动作、特效、美术素材，也不替代后续批次的独立复查。
 

