# Review entry: R3-B3 r3

Scope: B3's eight units and the shared mechanisms SkillCycle, MobilityLock, FormChange, RelationOrbit and poison.

Required independent checks:
- targeted B3 and visual fixture suite;
- full MC Fight suite with `--dir games/game-mcfight` so frozen packages are never rediscovered;
- TypeScript, build, and R3 combination audit;
- six mutation/revert checks in `freeze-check/mutations.json`.

Do not update `full-restoration-matrix.json` or sign an independent review from the program evidence.
