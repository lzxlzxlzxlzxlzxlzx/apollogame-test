# REQ-MCFIGHT-004 独立复核 — 2026-09-15

复核者：s4_drag_review（未参与施工）。范围仅公共 drag-place 自由部署扩展，不是 S4 最终复查，不签阶段。

## 冻结与复跑

隔离目录：`C:/Users/24652/Desktop/projects/apollpgame/mcfight-s4-drag-review-20260915`。由已准备 baseline 复制（排除 node_modules，独立副本建立依赖 junction），再覆盖当前 drag-place.ts、drag-place-free.test.ts、protocol/components/autochess.ts；不撤销共享目录、不修改 S3 冻结目录。

被测 drag-place.ts SHA256：`2b312055b43d816b00888284a619aa4d526544302d036f77f5cf8d73ec264b40`。

命令：`node node_modules/vitest/vitest.mjs run src/skills/tier2/drag-place.test.ts src/skills/tier2/drag-place-free.test.ts`。

旧 8 + 新 11 = 19 项通过，实际进程退出码 0。组合审计 27 系统，SCC 0，重复 ID 0，acyclic true。20 条可选排序引用涉及 15 个未安装系统；逐项在能力源找到对应系统，不是拼写失效引用。该组合仅 S3 战斗 + drag-place，不代表尚未完成的完整 S4 装配。

## 撤修验红

[原始日志与可复跑脚本](s4-drag-20260915/)。仅在隔离副本撤修；每次精确锚点命中数必须等于 1，否则脚本失败。

| 撤修 | 锚点数 | 测试实际退出码 | 恢复 SHA |
|---|---:|---:|---|
| 删除完整圆边界拒绝 | 1 | 1 | 相同 |
| 删除友军重叠拒绝 | 1 | 1 | 相同 |
| 删除 drop-zone 新自由部署阶段门 | 1 | 1 | 相同 |

现有断言能够检测相关实现失效。旧默认 hex / 自由落点行为由 8 项旧用例保持；新配置分支才改变自由部署及投放区阶段规则。完整圆外切、友军外切、拒绝保留旧部署位置、回席锚点、再次部署、关闭阶段与重新开门、NaN 输入、镜像等比缩放、新世界隔离均有生产执行覆盖。

读取与写入核对：新增几何决策读取 Shape、Transform、Tag；提交写 Transform 和 Tag；两系统阶段门读取 Flag，均在系统声明中列出。DebugTrace 为既有调试旁路，声明未列其读写；不可将调试日志当战斗调度数据。元描述 freePlacement 字段仍用 string 标签而协议实际为结构化对象，描述已明确链接协议，建议后续元数据类型支持时统一。

## 额外发现：非法邻居配置未闭合

隔离新增探针：将已部署 ally 的 Shape.radius 设为 -1（moving 半径为 1），拖到 ally 中心。期望无效占位拒绝；实际接受至 (-10,0)。独立文件 11 通过 / 1 失败，实际退出码 1，见 invalid-neighbor.log。原因是 occupied-circle 只查 radius finite，未查正数；半径和为 0 后距离严格小于 0 不成立。邻居位置与缩放的有限性、缩放非零同样未完整检查。

这是非法配置防御缺项，不等于合法 S4 参数的重叠拒绝失败。建议在公共入口统一拒绝非法占位几何并补固定回归，再就该小项复核；不扩大玩法范围。现有合法几何、阶段门及撤修检测证据成立，本报告不宣称公共能力所有输入均验收通过。

## 交付边界

完整回归由主程另行执行；已知旧 S3 基线 grid-move / pull-anchor 两失败，依据 `self-check/s4/old-baseline-probes.log`，不归本次改动但不能写全库全绿。本单没有 UI 绑定，不执行浏览器旅程。

REQ-005/006 仍待裁决；完整 S4 未完成、无 gate 或阶段签核。独立报告仅给出上述限定范围证据，不关闭其他请求。

## 当日修复后追加复核（保留以上首次发现）

主程已补邻居 radius 正数、位置/缩放有限且缩放非零校验，以及包含三种无效占位变体的固定用例。再次从共享目录同步两文件至同一隔离副本，运行旧测试、新测试、原独立失败探针：共 32 通过，退出码 0。其中固定回归为旧 8 + 新 12 = 20；另 12 为原独立探针文件（包含 11 条重复基础用例，不应作为 32 个独立场景宣传）。原负半径失败场景转绿。

在修复后版本再次执行全部三处撤修：每处锚点 1、测试退出 1、恢复 SHA 相同。最终 SHA 以附件 mutation-results.json 为准；首次版本 SHA 保留供复现历史。组合仍 27 系统、无 SCC、无重复；固定测试中的完整组合审计同步再次通过。

结论：REQ-MCFIGHT-004 本次限定公共扩展的独立复核通过；上述非法邻居退回项已修复复核。该结论不代表 S4 完成，不代签阶段，不豁免完整回归旧失败或其他待裁决缺口。最终运行日志为 drag-final.log。
