# S2 能力计划关 · 复查留档（独立第二轮：REQ-G109-001 结转判 + REQ-G109-003 首审）

- **复查人**：独立复查 agent（另开 fresh-context，**非**施工人；本 session 未改 `src/**`、`games/**`、`scripts/**`、
  `docs/design/game109/*.md`、`capability-gaps.json`、`pipeline.json`）
- **日期**：2026-09-18
- **复查对象**
  - **A. REQ-G109-001**（逻辑类 Effect 指针寻址）：**结转**。owner 2026-09-18 判词原文
    「`docs/design/game109/review/REQ-G109-001.md:69` 为准」→ 以该档 PASS 为准，本档不推翻、不重判。
  - **B. REQ-G109-003**（`Effect.whenGlobal` 全局条件门）：**首审**（此前无人复过，四步铁律从零走）。
    `Effect.when`（REQ-G109-002）与 hit 级施放判定同批同文件，一并复。
- **活树基线（sha256 我自己算，不照抄施工方）**

  | 文件 | sha256 |
  |---|---|
  | `src/skills/tier2/effect-apply.ts` | `c233525aabc4c972b0efaf4da1e3e79ec746f139ed6acdcbd5ba76b449510e14` |
  | `src/engine/protocol/components/logic.ts` | `9b4a508ece16ddc09b3eae3dab0e2a0dde8612825ad55eff28eb849963c1c0c7` |
  | `src/skills/tier2/effect-apply.test.ts` | `0ad204b016b93b03a413853fe3481c549acb7c5895ed6343357cf5c56388c001` |
  | `games/game109/probe.test.ts` | `b12c526269f2c89a4ca23954774672e895709ceed35060673b5baf2b9d5dfa2e` |

- **裁决：PASS**（minor 五项见 §6；无 P0/P1；S3 可开）

---

## 0. 范围核查（review-gates §四·每关首项）

mtime 窗内（2026-09-18 00:19–00:38）game109 施工方**碰过**的文件，逐一对边界栏（`requests.md:26/55/77`）：

| 文件 | mtime | 在边界栏 |
|---|---|---|
| `src/engine/protocol/components/logic.ts` | 09-18 00:19:59 | ✓（-002 边界栏「只给 `Effect` 加可选字段」） |
| `src/skills/tier2/effect-apply.ts` | 09-18 00:20:14 | ✓ |
| `src/skills/tier2/effect-apply.test.ts` | 09-18 00:21:07 | ✓ |
| `games/game109/probe.test.ts` | 09-18 00:36:52 | ✓ |
| `docs/design/game109/**`（brief/plan/requests/gaps） | 09-18 00:38 | ✓ |

**越界：无。**

- 「仍然不碰」清单复核：`src/skills/tier2/self-rule.ts` mtime **2026-09-15 18:51**、`condition.ts` **09-12**、
  `event-when.ts` **09-15** —— 全在开工前，**「只 import 一行不改」成立**（sha256 复核见 §2 末）。
- 易误判项（**不算本工越界**，供后人省事）：`src/skills/tier2/index.ts`（09-17 11:09）与 `vite.config.ts`
  （09-17 04:25）虽落在「近 36h」窗内，但**早于本工开工**，内容属另一在飞工件 mcfight r3
  （`knockback/hitbox/over-time` 导出、`frozen-source` 排除项）。判据=mtime + 内容，非「谁碰过」。
- 仓库根另有 mcfight 散件（`r3-*.ts`、`caster.ts`、`contact.ts`、`prefab.ts`、`spatial.ts`、`volley-emitter.ts`、
  `r3-b2-*.md`）与 `.vitest-req3.json`（见 §6-e），均非 game109 边界内文件。

## 1. 第一步 · 独立复跑（我自己跑）

| 项 | 命令 | 结果 |
|---|---|---|
| 单测（活树） | `npx vitest run src/skills/tier2/effect-apply.test.ts --reporter=json` 后**按文件路径**读 counts | **66 passed / 0 failed** |
| 探针 | 同上，`games/game109/probe.test.ts` | **19 passed / 0 failed** |
| 类型 | `npx tsc --noEmit` | `TSC_EXIT=2`，**2 条既有错**（`src/skills/tier2/knockback.ts:12:63` TS2352、`games/game-mcfight/r3-b4-public-capabilities.test.ts:7:125` TS2698），**0 新增** |
| 全库 | `npx vitest run --reporter=json` | suites 3958 / tests 10848 · passed 10804 / failed 42；**红文件 30（含我自己的隔离副本 2 个）→ 真红 28** |

- ⚠ **路径参数是子串过滤**（活教材）：`npx vitest run src/skills/tier2/effect-apply.test.ts` 会**同时**收进
  `docs/design/.../frozen-source-v2/src/skills/tier2/effect-apply.test.ts`，报 **2 files / 112 tests**（66+46）。
  核数必须用 `--reporter=json` 按 `testResults[].name` 逐文件读，不能读汇总行。
- P7 探针 stdout 原文（与 `requests.md` §6 引用**逐字一致**）：
  `[P7-a] energy=1 点荒地 → tile-a=tilled  energy=0`
  `[P7-b] energy=0 点荒地 → tile-a=wild  energy=0`
  `[P7-c] 已翻土格 → tile-a=tilled  energy=3`
  `[P7-d] 同拍点两格 → tile-a=tilled tile-b=tilled energy=0`
  `[P7-e] 两半直接对读 → tile-a=wild  energy=0（活没干成·体力却没了）`

## 2. 第二步 · 撤修验红（**隔离副本内**，实况全程未动）

配方（与 owner 硬约束一致；隔离目录 `.s2rev-sabotage/`）：

```
mkdir -p .s2rev-sabotage
sed -e "s#from './condition.js'#from '../src/skills/tier2/condition.js'#" \
    -e "s#from './self-rule.js'#from '../src/skills/tier2/self-rule.js'#" \
    -e "s#from '../score-trace.js'#from '../src/skills/score-trace.js'#" \
    src/skills/tier2/effect-apply.ts > .s2rev-sabotage/effect-apply.ts
sed -e "s#from './event-when.js'#from '../src/skills/tier2/event-when.js'#" \
    src/skills/tier2/effect-apply.test.ts > .s2rev-sabotage/effect-apply.test.ts
# 改副本（锚点整行替换），跑 .s2rev-sabotage/effect-apply.test.ts
```

| 变体 | 改法（副本内） | 结果 |
|---|---|---|
| BASE | 不改（搬来即跑） | **66 绿 / 0 红** ⇒ 副本保真 |
| A「全局门失效」 | 删 `if (ef.whenGlobal && !evaluateCondition(...)) continue;` | **3 红 / 63 绿** |
| B「self 门失效」 | `gatePass` 恒真（`!ef.when \|\| evaluateSelfCondition(...)` → `true`） | **6 红 / 60 绿** |
| AB（=施工方口径） | 两处同时失效 | **9 红 / 57 绿** ← 与施工方声称**逐字一致** |
| C「作用域错位」（**我加的·更强**） | 全局门改按源实体求值（功能错而非缺失） | **2 红 / 64 绿** |

- 红点归属（我逐名核对）：A 的 3 条全在 `-003` 块；B 的 6 条 = `-002` 块 5 条 + `-003` 块「全局过 ∧ self 不过」1 条；
  **AB 的 9 条 = -002 块 5 条 + -003 块 4 条**。⇒ 施工方「红的恰是两族的锚点（-002 的 5 条 + -003 的 4 条）」**准确**。
- C 的红点 = `-003` 块的「体力 ≥ 1 → 两道门都过 → 施于被点那格」+「全局门读的是**全局单例**、不是源实体」。
  ⇒ **作用域维度是机器钉住的**，不是只钉了「有一道门」。
- 红输出原文（AB，节选）：
  `AssertionError: expected 'sown' to be 'wild' // Object.is equality`
  `at ...\.s2rev-sabotage\effect-apply.test.ts:651:29` ← 栈里是**副本路径**，证明跑的确是副本、不是实况。
- 收尾：`rm -rf .s2rev-sabotage`（退出 0，仓库根已无残留），复核
  `effect-apply.ts = c233525a…`、`logic.ts = 9b4a508e…`、`effect-apply.test.ts = 0ad204b0…`、`probe.test.ts = b12c5262…`
  —— **与撤修前逐字一致**（我算的，与施工方声称的 `c233525a…` 独立吻合）。

## 3. 第三步 · 实证复现（自造反例撞语义边界）

自写 13 条对抗测试（放在隔离目录内，**只 import 活树 `src/**`**；验完随目录删除，配方见下），全部通过：

| 组 | 撞的边界 | 结论 |
|---|---|---|
| ADV-1 | 两门 AND **两个方向** + 正路径对照 | 全局过∧self 不过 → 不施**且不回落**改旁边格；全局不过∧self 过 → 不施；两门都过 → 施 |
| ADV-2 | **强判别式**：源实体的 `Resource(id=energy)=0`、全局 `wallet.energy=5`，同一棵条件分别喂 `whenGlobal` / `when` | `whenGlobal` 走全局（施）、`when` 走 self（不施）⇒ 两作用域**确实不同源** |
| ADV-3 | 多源信号逐源判定 | 两源都过→两格都施；一过一不过→只施过的那格；全局门不过→**全都不施**（全局门是 hit 级、不做逐源补偿） |
| ADV-4 | **短路**：`when` 塞「求值即抛」炸弹 `{kind:'and', of:undefined}` | 全局门不过 → `w.tick()` **不抛**；对照组（全局门过）→ **抛** ⇒ 全局门真的先判且短路，能证明不是「炸弹根本不炸」 |
| ADV-5 | 零回归 / 活引用 / fail-closed | 两门都不给 → 逐字旧行为（体力 0 也照施）；**同拍内 order 0 扣费改过的全局量，order 1 的 `whenGlobal` 看得到新值**（`lookup` 是活引用非值快照——S3 三拍握手正靠这条）；全局 id 不存在 → fail-closed 不施 |
| ADV-6 | 全 kind 通用 | `set-flag` / `modify-resource` / `destroy` 同过一道全局门（门不过则 Flag/Resource/DestroyRequest 三处全无变化） |

- 构造要点（可复现）：`new World()` + `effectApplyCapability.systems` 逐个 `addSystem`；`createEntity`+`addComponent`；
  `w.tick()` 一拍内完成 Update→Resolve→Commit。
- 结论：**语义与施工方声称一致**，且**不是「碰巧绿」**——ADV-4 用抛错探针把「先全局后 self」的**判定顺序**钉死，
  ADV-5 把「lookup 活引用」这条 **S3 依赖**单独钉死（此条不在施工方任何一条测试的射程内）。

## 4. 第四步 · 读告警（逐行读，不信退出码）

| 守卫 | 原文 | 退出码 | 归因（我的判读） |
|---|---|---|---|
| `node scripts/component-manifest-guard.mjs` | `[component-manifest] 当前 162 个共同零件 · 基线 155` / `✚ 新增：FormChange, KnockbackRequest, MobilityLock, ProjectileFlight, RelationOrbit, SkillCycle, VolleyPlan` / `COMPONENT-MANIFEST: FAIL` | **1** | 7 个新零件名全属 mcfight r3，**无一 game109**（game109 只加字段不加零件，边界栏原话成立） |
| `node scripts/docs-ref-guard.mjs` | `FATAL ERROR: Ineffective mark-compacts near heap limit Allocation failed - JavaScript heap out of memory`（4 GB 上限，~150 s） | **134** | **既存**、与本工无关，但施工方的机制描述不精确——见 §5-c |
| vitest 全库 | 42 failed / 28 红文件 | — | 见下 |
| `src/assembly/declaration-audit.test.ts`（活树，7 测 2 红）原文 | 10 条瞒报全在 `tier2\hitbox.ts`(MobilityLock/ProjectileFlight)、`tier3\caster.ts`(SkillCycle/State/VolleyPlan)、`tier3\flow.ts`(Shape/Transform)、`tier3\prefab.ts`(Hierarchy)、`tier3\volley-emitter.ts`(DestroyRequest/Transform)；相位棘轮只多一项 `p1:projectile-flight` | — | **零条涉及 `effect-apply.ts` / `logic.ts` / game109**；`effect-apply` 仍稳在 `p20` 桶内（diff 里是上下文行，非 + 行） |
| `src/assembly/registry-guard.test.ts` | `expected [ { id: 't2-knockback', …(1) }, …(3) ] to deeply equal []` | — | mcfight 未注册的 4 件 |

**28 红文件分桶（我按路径实点）**：`frozen-source-v2` **17** · 其它冻结点 2（`r3-b1-freeze`、`r3-b3-freeze-…-r3-complete/review-input`）
· 仓库根散件 3（`r3-b2-{identity,production,runtime-wiring}.test.ts`）· 活树 6（`scripts/build-component-map.test.mjs`、
`scripts/component-manifest-guard.test.mjs`、`scripts/docs-ref-guard.test.mjs`、`games/game-mcfight/s4-combat.test.ts`、
`src/assembly/declaration-audit.test.ts`、`src/assembly/registry-guard.test.ts`）。
⇒ **活树红点零条可归到 -001/-002/-003**；「零回归」不是「看着无关」，是**逐条点到 mcfight/冻结点**。

## 5. 与施工方证据对账

**对得上的（我独立复现）**：单测 66 ✓ · 探针 19 ✓ · tsc 0 新增 ✓ · 撤修验红 **9 红/57 绿** ✓（含「-002 5 条 +
-003 4 条」的分族）· 实况 sha256 `c233525a…` ✓ · 「self-rule.ts 一行不改」✓ · 三拍握手 P7-a~e 原文 ✓ ·
「只加字段不加零件 ⇒ 零件守卫零影响」✓ · 「28 文件红」**总数** ✓。

**对不上的（明写）**：

- **a. 红文件分桶的「20」**：`requests.md:87` 称「28 文件红（其中 **20** 个是 `frozen-source-v2/` 下的）」。
  我实点 **17**。差 3 = 仓库根那 3 个 `r3-b2-*.test.ts`（suite 级红、0 断言），另 2 个在别的冻结点目录
  （`r3-b1-freeze`、`r3-b3-…-r3-complete/review-input`）。**总数 28 与结论（全是 mcfight/冻结点、无一属本工）我复核成立**，
  但对不上的是**分桶口径**——若后人照「20」去数，会找不到那 3 个。
- **b. `docs-ref-guard` 的机制**：`requests.md:93-94` 归因「冻结点快照的体积拖死」「任何走 `docs/**` 的守卫都会被拖死」。
  结果对、机制不精确：该守卫 `DEFAULT_SCAN_DIRS = ['docs/roles','docs/playbooks','docs/qa']` **不含 `docs/design`**，
  OOM 是 **`fs.globSync()` 被宽通配 token（`docs/design/**`、`src/**` 等）拉去枚举含 `frozen-source-v2/node_modules`
  的整棵树**造成的；我实测把扫描范围缩到只剩 `docs/design/game109` 三份文档**仍 OOM（exit 134）**，
  触发者是 `requests.md:94` 自己写下的 `docs/**` token。⇒ 「本工无关」成立，但**本工文档也给这颗雷添了一根引线**。
- **c. 「20/17」之外的第三个不一致——`.vitest-req3.json`**：仓库根留着一个临时产物
  （mtime 2026-09-18 00:22:51，129 tests：`probe.test.ts` **14** + `effect-apply.test.ts` 66 + 冻包 46 + `r3-b4` 3）。
  它是施工方 `-003` 中途的快照，**早于 `probe.test.ts` 最后一次落笔（00:36:52，19 探针）**，
  故其 `14` 与 §6 声称的「探针 19」**对不上**。不是假证（不是他们引用的证据），但**是没清干净的临时文件**，
  且数字已陈旧——若被后人当证据读会误导。建议施工方 `rm`（我不越界代删）。

## 6. minor（PASS 的附带条件，逐条）

- **a. `reads` 申报未含 `StringVar`（承 -002 债 a）**：`when`/`whenGlobal` 经条件树可读 `StringVar`
  （`condition.ts` 的 `string` 叶），`effect-apply` 的 `reads/writes` 两栏都没有它；`declaration-audit` 因访问是**间接**的
  （走求值器而非直呼 `getComponent`）而不报。施工方已把「reads 无法如实补齐（有 game108 环实证）」登记为债——
  我**结构性复核过**该环：`craft-recipe` `reads:['CraftRecipe','Signal','Resource']` / `writes:['Resource','Flag','State']`
  （`src/skills/tier2/craft-recipe.ts:51-52`），而 `effect-apply` `writes` 含 `Resource`，故给 effect-apply 补
  `Resource` 读侧确会闭合二环。⇒ 留债合理，但**口径要写进 spec**（「条件树读集」如何申报），否则下一个人还会撞。
- **b. 冻包 v2 逃过 `vite.config.ts` 的 exclude**（承 -001 minor b，仍未解）：`frozen-source-v2` 与 exclude 里
  的 `frozen-source/**` 不匹配 ⇒ 全库跑多收 46 条 + 17 个红文件。本关不阻断，但**每次全库复查都要靠人脑扣除**。
- **c. `validate-references` 对逻辑类 `targetEntity` 仍零校验 + `@signal-source` 存量误报**（承 -001 minor c，未解）。
- **d. 重复 `fsmId` 病理语义分歧**（承 -001 minor d，未解）。
- **e. 临时产物未清**：`.vitest-req3.json`（见 §5-c）。另建议 future 用 `--outputFile` 写进临时目录而非仓库根。
- **f. `Effect.order` 语义扩张 + 无「无条件快照」件**：`-003` §7-a/b 自陈债务，我复核「探针可佐证、spec 无明文」
  属实。**S3 装配务必照三拍握手（order 0/1/2 + 临时态 `busy`）**，否则同拍两半对读必自锁（P7-e 已留陷阱指纹）。
- **g. `game-pipeline.mjs review --note` 静默截断到 500 字符**（本关实测）：我按「note 须含哈希/复跑/验红/告警/minor」
  写的第一版 **1068 字符被无声截到 500**（`reviews.S2.note.length=500`，尾部断在半句「相位棘」），落账无任何提示。
  ⇒ 本档承载完整结论，机读 note 只放 ≤500 的提要；**建议守卫加一条「note 超长即报错」**（`scripts/**` 在边界栏外，
  本关只上报不处理）。
  **留档说明**：`pipeline.json.history` 里会出现**两条** `review:S2`——第一条 `2026-09-17T17:21:31.872Z` 是
  被截断的那版（note 500 字·尾句断在「相位棘」），第二条 `2026-09-17T17:22:43.805Z` 是我用 ≤500 提要覆盖后的**正式落账**；
  两条都是本复查人所为，**不是重复/伪造记录**。

## 7. 结论

- **A. REQ-G109-001**：按 owner 2026-09-18 判词结转 **PASS**（口径=`review/REQ-G109-001.md:69`），不重判。
- **B. REQ-G109-003**：**PASS**。四步铁律全部留原文：复跑 66/19 全绿、tsc 0 新增；撤修验红 9/57 与施工方一致
  且我另加「作用域错位」变体（C，2 红）把**全局作用域**这一维也钉住；13 条自造反例（含抛错探针与活引用实验）全过；
  告警逐条归因，**活树红点零条属本工**。
- 唯一「有条件」之处是上列 minor：**没有一条阻断 S3**；其中 **f 是 S3 的照办项**（三拍握手），**b 是全库复查的长期噪音源**，
  **a 要写进 spec 口径**。
- 本档为独立复查断言；机读记录仅由 `scripts/game-pipeline.mjs review` 写入，本档不代替之。
