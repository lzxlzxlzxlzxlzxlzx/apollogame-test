# game109 · S3 复查报告（独立 · fresh context · 非施工人）

- 复查人：独立复查 agent（S3 · fresh context · 非施工人）
- 日期：2026-09-18
- 复查边界（已遵守）：**只读 + 只写复查归档**。未改动 `games/game109/**`、`src/**`、`scripts/**` 的任何文件；未改动 `docs/design/game109/` 下的策划文档；`public/games/game109/pipeline.json` 只经 CLI 写入。
- 撤修安全边界（已遵守，逐字）：sabotage-verification **只在隔离副本** 上进行，从未就地改实况 `src/skills/tier2/effect-apply.ts` 或其他实况文件；副本 `.req4-sabotage-s3review/` 用后即删；报告末尾附前后 sha256 一致证明。

## 判词：CONCERNS（有条件过）

**依据**：game109 S3 交付本体（8 文件）逐件 sha256 与施工人申报逐字一致、我全部自跑一遍全绿、六条怀疑全部用真跑/真截/真读输出结清且全部站在施工人一边、五轮单元级撤修 + 一轮浏览器级撤修全部验红且锚点断言证明「打到了目标」。**未发现任何功能缺陷。**

**CONCERNS 只针对交付面之外的两笔账（不阻塞 S3 结论，但需 Lead 认领）**：

1. **门禁工具链在 S3 窗口内被改且未申报**：`scripts/lib/render-harness.mjs` 与 `scripts/render-probe.test.mjs`（mtime 2026-09-18 03:40）不在声明的 8 文件交付清单里，而它们正是本阶段 gate 所依赖的渲染/点击探针本体。文件内含理由注释（剥 ANSI SGR 再匹配端口，修 Windows 上编译期游戏 S3/S4/S5 渲染门「全部假红」），且我复跑随附测试（`npx vitest run scripts/render-probe.test.mjs` → 2 文件 / 36 测全绿）。我另用**自己的**浏览器harness（不经 `render-probe.mjs` 的判据代码，只 import `detectBrowserRuntime/startDevServer/deepLinkQuery`）独立复现了同款判据与同款结论，故判定该改动对本阶段判据语义**无害**；但「施工期改动批改工具本身」这件事必须由 Lead 明确认领，并建议对 03:40 之前已过门的其他编译期游戏在新工具链下重跑一次 gate 以确认无回归。
2. **`games/game109/game109.skeleton.test.ts:254` 标题名实不符**：标题写「没选播种（还握着锄头）→ 点已翻土的格子不播种、也不扣体力」，正文实际是 `pickTool(e, 1)`（选了播种）+ 点在 **wild** 格（`work(e,0,0)` 后断言仍是 `WILD`）——测的是「播种工具 + 荒地」而不是「锄头 + 已翻土格」。断言本身正确、且工具前置门确已被撤修 S2 验真（见下），**不是覆盖缺失，是命名误导**，请顺手改正以免后人误信该用例已覆盖「tilled 格被播种」这一路径。

---

## 一、复跑的真实输出（全部由我本机重跑，非引用他人报告）

### 1. 单测：`npx vitest run games/game109`

```
 Test Files  2 passed (2)
      Tests  40 passed (40)
   Start at  05:03:07
   Duration  7.64s (transform 4.01s, setup 0ms, collect 7.98s, tests 1.40s, environment 0ms, prepare 1.98s)
```

（`game109.skeleton.test.ts` 21 测 + `probe.test.ts` 19 测 = 40。红色数 0，跳过数 0。）

### 2. 门禁：`node scripts/game-pipeline.mjs gate game109 S3`

```
GATE_EXIT=0
{"ok":true,"slug":"game109","stage":"S3","exit":0,"summary":"编译期游戏免 manifest 校验 · ✓ 渲染探针过（截图 public/games/<slug>/probe/S3-render.png） · ✓ 点击打穿（6 个控件 · 4 个点后 DOM 真的变了 · 零控制台 error）","at":"2026-09-17T21:04:36.925Z","gameHash":"67659191c9b158b8"}
```

- 我独立重算的 `gameHash` = `67659191c9b158b8`，与账本一致。
- 门禁自跑的探针产物为最新：`public/games/game109/probe/S3-click-gate.json`、`S3-render.json`、`S3-render.png`（105,675 B，均 05:04 生成）。
- 账本历史保留着首轮真红（exit 1 @ 20:22:53），其后 20:33:49 / 20:46:54 / 21:04:36 三次 exit 0 —— 红不是被抹掉的。

### 3. 类型检查：`npx tsc --noEmit -p tsconfig.json`

```
TSC_EXIT=2
games/game-mcfight/r3-b4-public-capabilities.test.ts(7,125): error TS2698: Spread types may only be created from object types.
src/skills/tier2/knockback.ts(12,63): error TS2352: Conversion of type 'Map<string, Component>' to type 'DestroyRequest' may be a mistake ...
```

exit 2，但只有 **2 条既有错**，均与 game109 无关（`game-mcfight` 旧测试、`knockback.ts` 旧转换）；**game109 相关错误 0 条**。

### 4. 点击门判据原始件（`public/games/game109/probe/S3-click-gate.json`）

```
controls: 6 · changed: 4 · consoleErrors: []
hits: btn-till(changed=false) btn-sow(true: tool/till/sow) btn-water(true: tool/sow/water)
      btn-reap(true: tool/water/reap) btn-sleep(true: day) btn-sell(changed=false)
bindWiring: { ok: true, detail: "未用 props.bind" }
```

### 5. 告警逐行读（WARN 走 stderr、不改退出码）

- **gate 自身 stdout/stderr 里 `WARN` 命中数 = 0/0**（`grep -c WARN` 对两个流都是 0）。门禁没有被我漏读的告警。
- 唯一反复出现的告警来自 vitest stderr，逐字如下：

```
[topological-sort] phase 0：检测到由**组件推断边**闭合的定序环 [state-sync, flow]（闭环组件：State）。
环上无自相矛盾的显式申报 → 按确定性平局裁决（注册序/tier 序·已服从既有 runsAfter/runsBefore）定序为：
state-sync → flow。此顺序仅保证可复现、不保证合语义：要指定先后请用 runsAfter/runsBefore 显式申报覆盖，
或按语义把系统拆到不同 phase（REQ-CYCLEHAZ）。
```

**对该告警的独立判定：引擎既有诊断，不是 game109 缺陷。** 证据：跑 `npx vitest run src/engine/core`（纯引擎、不含任何游戏代码）时同样出现（命中 2 次）；它由 `src/engine/core/topological-sort.ts` 在「世界里的组件推断边闭合成环」时发出，取决于世界组件构成而非 game109 的代码写法；而 game108 的测试跑下来命中 0 次（说明它不是无差别噪声，也没被 game109 引入成新病）。它只提示「排序仅保证可复现」，且因走 stderr 不影响退出码。

---

## 二、六条怀疑的逐条结论（全部真跑结清）

### ① 点击门的 `changed` 是否真穿透到世界（还是 HUD 自画自己）？—— 真穿透

浏览器级实证（真 Chromium + 真 dev server + 真鼠标点击；给 `game109.ts` 在**内存里**补一行挂出 `globalThis.__g109Engine`，**盘上文件零改动**，锚点命中校验：找不到 `engine.start();` 就记错误）：

- 开局 `world` 读数：energy 20 / gold 0 / day 1 / tool 0，`tile-r0c0` 的 State = `wild`。
- DOM 点 `#g109-hud-btn-sow`（工具箱 id 指向 `toolbox` Resource）→ **世界 `toolbox` Resource 0 → 1**，同时 `#g109-hud-tool` 文本随之更新。也就是说 DOM 点击改的是世界真值，HUD 只是跟着读。
- 走完整玩家链（DOM 选工具 + 画布点格子）`till → sow → water → sleep`：`tile-r0c0` State `wild→tilled→sown`、`Flag.active` 置位、体力逐次 −1、切天后 day 1→2 —— 世界状态逐步与设计一致，控制台 error 0。
- 反向证伪（浏览器级撤修）：在内存里把 `blueprint.ts` 的 `kb-*` KeyBinding 建行整段删掉（**锚点断言 `kbLinesRemoved === 1`**，实测世界侧 `KeyBinding` 计数 = 0），再逐字复刻 click-probe 的判据重放一轮点控件：`controls 6 / changed 0`（门会真红），而同一页面里**画布路径仍能干活**。→ 点击门咬的确实是「DOM → 世界」这条链；若点击只是 HUD 自画，撤掉世界侧消费者后它照样会「变」，实测是 0 变。

### ② HUD 路径与画布路径是不是同一个 Effect（还是两份逻辑副本）？—— 同一份

- 逐信号清点：每个 `SIGNALS.WORK` 只对应一组 Effect 实体，DOM 按钮 `props.action` 与画布 `Clickable.action` 使用的是同一批信号常量，不存在「HUD 专用第二套效果」。
- `kb-*` 6 个 `KeyBinding` 只覆盖会写全局量的动作（睡眠/卖货/选工具类），且都不设 `source`/`phase` —— 与 `src/skills/tier2/keybind.ts` 里「空字符串 `source` 抛错、`phase` 缺省即全相位匹配」的既有约定一致；`targetEntity: '@signal-source'` 的指针寻址不会被 kb-* 抢走（kb 实体不是格子）。
- 浏览器里两条路径产生的世界变化**逐字段相同**（同一 `toolbox`、同一 `State`、同一体力账）。

### ③ `FIELD_H` 826 → 910 的涟漪 / 隐藏假设？—— 自洽，无隐依赖

- 全仓无任何地方再硬编码 826；game109 无 golden/blessed 状态文件、S2 期的 `probe.test.ts` 不含几何断言（逐字查过），不存在会被这条改动打脸的基准。
- 屏幕实测：画布底部动作条底边 802 < HUD 条顶边 826，HUD 条（84 高）落在画布下方，与 `mountHost({fieldW, fieldH: VIEW.h, bottomBarH: HUD_H})` 的意图一致；截图（`S3-render.png`）目视无重叠/裁切。
- `networkidle` 能稳定 settle（`networkidleSettled = true`），没有出现「永远打不完的加载」。

### ④ 全仓还有没有别的 `not.of` 单值陷阱？—— 没有第二处

- 机制复核（读源码）：`src/skills/tier2/condition.ts` 与 `self-rule.ts` 的 `switch (expr.kind)` **都没有 `default`**。`and`/`or` 是 `expr.of.every(...)` / `.some(...)`（给对象会当场抛错=响亮失败），而 `not` 是 `!evaluateCondition(..., expr.of)`，`expr.of.kind === undefined` 时返回 `!undefined === true` —— **静默恒真**，这就是坑的全部。
- 全仓文本扫描（`games/**` + `src/**`）：所有 `kind: 'not'` 的 `of` 都是**单个表达式**，无一处写成数组；`and`/`or` 全部正确地传数组。game108/game-f/game-mcfight/game-e/game102 等一并扫过，零命中。
- game109 侧：`blueprint.ts:75` 的 `notWatered` 与 `:115` 的 `{ kind:'not', of: isRipe(crop) }` 都带 ⚠ 注释点明「单个 expr，不是数组」；`game109.skeleton.test.ts:183` 留有形状走查守卫。
- 撤修 S5（把 `not.of` 改成数组）复现出**一模一样**的症状，且守卫测试以精确消息点出站位（`water-claim-fx.Effect.when[1]`），另有 `expected 16 to be 17` 的双扣体力表现 → 陷阱真的存在、守卫真的咬得住。

### ⑤ `ui.update` 每帧更新会不会重演 game108 的「每帧新皮肤 → mountUI 重建 → networkidle 永不落定」？—— 不会

- 代码面：`src/ui/components/server.ts` 的 `uiOwnSame` 先做 type/props/layout 的 JSON 等值比较，`reconcileNode` 只对差异节点动手 —— 未变节点不触碰 DOM。
- 实测面：无点击对照跑 12 步 × 250ms（同一套 SNAP 判据、扣噪声），`noop_pass_noisy_ids = []` —— 静置期间**没有任何节点在动**；`networkidleSettled = true`；全程控制台 error 0。
- 结论：game109 的 `engine.subscribe(redraw)` 是「状态变才重画」，不是每帧新皮肤。

### ⑥ btn-till / btn-sell「首点不动」是真实现状还是接线断了？—— 真实现状，不是断线

- `btn-till`：起手 `tool` 已是 0（till），点它 = 重复选同一工具 → 快照 diff 为空（幂等）；改选别的工具后再点回来（`sow` → `till`）**确实会变**：`g109-hud-tool` 文本 + 两枚按钮标签一起变 —— 线是通的，只是首点无差异。
- `btn-sell`：空背包卖货 = 卖 0 件 → diff 为空（真实语义）；向世界注入 `inv-carrot = 2` 后再点，`g109-hud-gold` 变化且**世界 gold = 24**（= 2 × 胡萝卜价 12）—— 线是通的。
- 结论：两枚控件首点不动 = 设计上的幂等/零收益，非假绿。（副作用提示见「对不上」第 5 条：点击门只求「≥1 个控件有变」，这两枚因此永不被该门正向验证，靠单测兜底。）

---

## 三、撤修验红：锚点断言与前后 sha256

**安全边界**：全部撤修都在隔离副本 `.req4-sabotage-s3review/`（game109 目录的一份拷贝）内进行，**从未触碰实况文件**；副本首先跑基线（21/21 绿）确认「副本本身是好的」，撤修后即删除。

每轮撤修都先跑**锚点断言**（证明撤修打在了目标上、而不是把文件改坏），再跑全量：

| 轮 | 撤修点（副本内） | 锚点断言与实测 | 结果 |
|---|---|---|---|
| S1 | 压平工作链 `order`（三拍握手的定序） | `distinct orders = [0]`（断言压平后只剩 1 种且 = 0） | 锚点通过；**6 测红**，含 `expected 20 to be 19`（活干了、体力没扣） |
| S2 | 删掉工具全局门 `whenGlobal: toolIs(...)` | `仍读 tool 的拍数 = 0`（原始为 6 claims + 8 settles 类；`work().length ≥ 10` 保证没删空） | 锚点通过；**6 测红**，含 `expected 'sown' to be 'wild'` |
| S3 | 删掉体力全局门 `whenGlobal: hasEnergy` | `仍读 energy 的拍数 = 0`（原始 = claims 6 + spend 1 = 7） | 锚点通过；**2 测红**，含 `expected 'tilled' to be 'wild'` |
| S4 | 删掉 `kb-*` 具名生产者（DOM 路径世界侧消费者） | `kb-* 实体数 = 0`（原始 6）；同时 `btn-*` 仍 = 工具数 + 动作数（证明只砍了 DOM 路径） | 锚点通过；**2 测红**：kb 生产者 `expected +0 to be 6`、DOM 路径 `expected +0 to be 2` |
| S5 | `not.of` 改成数组（④ 的陷阱） | 守卫消息精确点名站位 `water-claim-fx.Effect.when[1]` | 锚点通过；**2 测红**，含 `expected 16 to be 17`（双扣） |
| 浏览器级 | 内存里删 `blueprint.ts` 的 `kb-*` 建行（盘上零改动） | `kbLinesRemoved = 1`；世界侧 `KeyBinding` 计数 = 0 | 锚点通过；点击门复判 `controls 6 / changed 0`（真红），画布路径仍可干活 |

**前后 sha256 一致（撤修全程未污染实况仓库）**：

```
e95e4629c39b7794b41c9f5d986a00472c9da642cca758cc57fd4a8abe1cafe0  games/game109/blueprint.ts
f01aa6cc0c4a164d58d53c45414f1dc52bfc16c64b3f226a6c94f5061e90401f  games/game109/data.ts
323ad1a29e2c136ee4477af32ddd022749c14575daa13f0c029c0da150a81377  games/game109/game109.skeleton.test.ts
165aa212687fff67fa01fce6a0f361eceeef1d9042bb082c522d2dc2aecd82fa  games/game109/game109.ts
a104c6de6eb12545f57ec3740dcde2960911f9609b7ca2dae09b3ad9ea7a6f10  games/game109/hud.ts
3c1e2ad538e43a7ea9b790282c08ffb31f18260d447dbf6db590b22e2cf0dba8  games/game109/index.ts
b12c526269f2c89a4ca23954774672e895709ceed35060673b5baf2b9d5dfa2e  games/game109/probe.test.ts
2c88ef78871185ebb32f85a2bf0918fff326699f9dcc297760aefa90439b1aed  games/game109/theme.ts
```

策划文档（复查后重算，与 S2 复查绑定输入一致）：

```
0d1e491a9532d8985d603df783d01a75ae8019bfbdfbbb9bf10119f6ca170f5b  docs/design/game109/brief.md
30d1542ab7ea54157ca45ef9bb4b5fc732f8cc68adc0102a14fcc8e8e6cbee75  docs/design/game109/capability-gaps.json
fd303ba10613e0acb9cfe447dd733c2d0f87339d67d4be5ad75ece7e3114b076  docs/design/game109/capability-plan.md
72d5f0a5f00824386f7450a314d04ba3d4120ff2cdabee04b81679f3ecd7980b  docs/design/game109/requests.md
```

**S2 复查未被作废**：我重算 `stageReviewHash('game109','S2')` = `1aa02c0219e57d0b`、`gapsHash` = `30d1542ab7ea5415`，与 S2 复查落账值逐字相同 → 前后一致，S3 未触发重新锁死。

**新游戏注册（编译期游戏的头号坑）已双点核过**：`src/launcher/game-runner.tsx:96` 的 loader 映射 `'game109': () => import('@games/game109/game109.js')` 与 `src/launcher.tsx:216` 的货架条目同时在位；浏览器实测能经深链路真正装载起 game109（否则上面所有点击实证都无从谈起）。

---

## 四、我对不上的地方 / 需 Lead 定夺（不夸大、不编小毛病）

1. **`scripts/` 在 S3 窗口内被动过（最重要的一条，也是本次判 CONCERNS 的主因）**：`scripts/lib/render-harness.mjs` 与 `scripts/render-probe.test.mjs` mtime = 2026-09-18 03:40，而声明的交付是 `games/game109/` 下 8 个文件。该改动有文件内注释说明理由（vite 5 在 Windows 经 picocolors 输出 ANSI，`Local:` 端口行匹配不上 → 20s 超时 → 编译期游戏 S3/S4/S5 渲染门「全部假红」；剥掉 SGR 即恢复匹配，Linux 行为不变），并随附测试（我复跑 2 文件/36 测全绿）。我另行独立复现了同款判据，判定**判据语义未被削弱**；但「批改工具被批改对象同期修改」这件事请 Lead 明确认领，并建议对更早过门的其他编译期游戏在新工具链下补跑一次 gate。
2. **简报点名的「六件策划文档」与实际目录对不上**：`docs/design/game109/` 下只有 `brief.md` / `capability-plan.md` / `capability-gaps.json` / `requests.md` 四件；`gdd.md`、`s1-s2-program-handoff.md`、`s1-s2-program-evidence.md` **不存在**。`stageReviewHash` 对 `S2_REVIEW_INPUTS` 的缺失项是跳过的，因此 S2 的复查指纹实际只绑在存在的子集上 —— 现记录值与我重算一致（S2 未被作废），但「六件」这个说法与仓库事实不符，且意味着那三份文档若日后补入，S2 复查会比现在覆盖得更宽。非 game109 的问题，属账目口径需对齐。
3. **`games/game109/game109.skeleton.test.ts:254` 测试标题与正文不符**（详见判词第 2 条）。
4. **死数据（均有注释、均无行为影响）**：`theme.ts:51` `BTN_TINT.toolActive` 与 `theme.ts:53` `READOUT_TINT` 只有定义、无读取（后者自注为 canvas 侧预留）；`data.ts` 的 `ToolDef.hint` 无读取、`ToolDef.cost` 只在 `data.ts:164` 的注释里被声明为 `BALANCE.actionCost` 的「逐工具副本」（实际读到的是 `BALANCE.actionCost`，逐工具字段无人读）；`CropDef.asset` / `ToolDef.asset` / `license` 无读取（注释标为 S6 元数据，其中 license 注释自认「署名机制未立项」= 已知缺口，非隐藏）。
5. **点击门判据存在结构性盲区（建议 S4 起补，非阻塞）**：门只要求「≥1 个控件点后 DOM 变化」，而 `btn-till`（幂等）与 `btn-sell`（空背包）按语义**永远不会变**，故这两枚控件在门里永远拿不到正向验证；它们目前由单测兜底（S4 撤修已证明这些单测会真红）。若要门也咬它们，需要「注入初始条件（如给背包 2 根胡萝卜）后必须有变」这类分档判据。
6. **我自己的 CLI 复跑重生的产物**（预期行为，非异常）：`public/games/game109/probe/S3-click-gate.json`、`S3-render.json`、`S3-render.png` 与 `public/games/game109/pipeline.json`（history 增加一条 gate 记录）。

---

## 五、残渣自检

- 隔离副本 `.req4-sabotage-s3review/` 已删除；仓库内已无任何撤修产物（`ls` 与 Glob 双查）。
- 8 件交付 + 4 件策划文档 sha256 复查前后一致（见上）。
- 我使用的临时脚本（浏览器实证、锚点生成器）全部位于仓库外：`%TEMP%\claude\g109-s3-verify\`。
- 本次复查对仓库的写入仅两处：本报告 + `pipeline.json`（经 CLI）。
