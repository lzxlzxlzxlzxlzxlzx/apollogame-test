# game109 · S3 复查报告（2026-09-20 清闸轮 · 独立 · fresh context · 非施工人）

- 复查人：独立复查 agent（S3 复查·fresh context·非施工人）
- 日期：2026-09-20
- 背景：S7 领工声明（`docs/design/game109/requests.md:1880-1911`）本轮施工面 = **清闸**：重跑 S3/S4/S5 机器门 + 派独立复查清 S3 复查门 + 重启 5173。无游戏文件改动。
- 写作边界（已遵守）：只写两处——`review` 落账（CLI）+ 本文件。未改 `games/**`、`src/**`、`scripts/**`、`public/**`、`docs/playbooks/**`；未跑 `gate`/`reopen`/`signoff`；未跑 `game109-art-requirements.mjs`（人门件）。
- 撤修安全边界（已遵守）：全部撤修在仓库外 `%TEMP%/g109-s3-sabotage/` 副本上进行，从未改实况文件；副本用后即删；实况哈希前后一致（见 §五）。

## 判词：CONCERNS（有条件过）

交付本体核证全部站在施工方一边（证据真、测试咬、内容纯数据），但两条记录在案：

1. **「无代码走私」守卫结构上抓不住函数走私**（本清单第 2 条的机器面）：唯一守卫
   `game109.skeleton.test.ts:154` 的 `JSON.stringify(entities)` 不抛——`JSON.stringify` 对函数值是
   **静默丢弃**而非抛错。撤修 A 在 wallet 实体上走私一个函数，49/49 全绿（详见 §三 A）。当前内容
   经我逐行读为纯数据（零函数走私），但「无走私」今天靠的是人读，不是机器断言。建议补一条
   「深走实体树断言零 function 值」的守卫（改 `games/game109/**` 测试文件即可，不涉引擎）。
2. **探针「零控制台 error」判据把启动器级故障算成游戏红**：4000 API 后端现宕机，启动器
   `src/launcher.tsx:294-297` 的「API 状态灯」fetch `/api/generate/providers` 经 vite 代理
   （`vite.config.ts:101-102`）回 500，被浏览器记为 console error。我在 5173 上实测抓到 4 条；
   render-probe/click-probe 的判据（`scripts/click-probe.mjs:90-91`、`interpretClickProbe`）对
   console error 是**零容忍** ⇒ 现在重跑 S3 门会因与 game109 无关的原因假红。07:51 门跑时后端
   还在（落盘证据 `consoleErrors: []`），故「证据新鲜」在钟表意义上成立、但**有环境时间炸弹**。
   建议探针过滤启动器壳层错误或状态灯失败软降级（属 `scripts/**`/`src/**`，本复查边界外，报 Lead）。

另有一条既存小尾巴（非本轮新增，不阻断）：上一轮 S3 复查两笔 CONCERNS 的处置核对——
① `scripts/lib/render-harness.mjs` S3 窗口改动已由 Lead 在 S4 领工声明哈希表认领
（`requests.md:553`）✓；② `game109.skeleton.test.ts:254` 旧标题「没选播种（还握着锄头）…」已从
实况文件消失，现为 `:383`「工具前置门：选了播种 → 点荒地不播种、也不扣体力（前置是已翻土）」，
标题与正文一致 ✓。

---

## 一、范围核查（清单第 1 条·对照 S7 领工声明）

- 非 git 仓库 ⇒ 按领工声明的哈希基线对照（`requests.md:1884-1888`）。实测 9 文件 sha256 前 12 位：
  `blueprint.ts 57c387875484` · `data.ts 4bfebe50ec23` · `game109.skeleton.test.ts 9ca1a02ff6c7` ·
  `game109.ts a701166ddd66` · `theme.ts 191e67db7368` · `hud.ts 658b7fb49b8b` ·
  `index.ts 3c1e2ad538e4` · `probe.test.ts b12c526269f2` · `acceptance-adapter.ts 73477f59688c` ——
  与声明基线逐字一致；`public/games/game109/art/index.json` = `729e6d1ca39b` 亦一致。
- 全部文件 mtime ≤ 2026-09-19（blueprint/theme/skeleton test 为 09-19 03:49），本回合（09-20）零改动。
- 结论：**无越界**。本回合唯一落账动作就是「清闸」本身，与声明一致。

## 二、独立复跑（四步铁律 ①）

1. **单测**：`npx vitest run games/game109/` → **2 文件 / 49 测全绿**（probe 19 + skeleton 30），exit 0。
2. **门禁证据（只读核·未重跑门）**：账本 `public/games/game109/pipeline.json` history 末条 =
   `gate S3 exit 0 @ 2026-09-20T07:51:26.342Z`；产物 `probe/S3-render.json`（07:50:52Z·gameHash
   592645fda91ea522）、`S3-click-gate.json`（07:51）、`S3-render.png`（196,990 B·09-20 15:50 本地）
   —— 均今日新落。
3. **截图非空白自证**：我用自己的 PNG 解码器（不 import 探针代码）解 `S3-render.png`：
   1280×800 灰度（colorType 0），mean 15.47，**variance 3355.87**（阈值 15）⇒ 非空白成立。
   （探针记 2974.45，口径略异、同一量级，两者都远在阈值之上。）
4. **点击「点后 DOM 真的变了」独立复核**：我自写 harness（%TEMP%·不 import click-probe.mjs），
   真 Chrome 接 **owner 令重启的 5173**，同款判据（自身直接文本+内联宽+禁用态快照、12×250ms
   不点对照趟扣噪声、逐控件点击）：**6 控件 / 4 变**，changedIds 与落盘证据**逐字一致**
   （sow→tool/btn-till/btn-sow；water→tool/btn-sow/btn-water；reap→tool/btn-water/btn-reap；
   sleep→day）。且 sleep 后世界日期读数 **1→2**（「第 2 天」）——DOM 变化由世界状态驱动，
   非 HUD 自画。艺术索引 `5173 /games/game109/art/index.json` = 200。
5. **load+2tick**：`game109.skeleton.test.ts:359`「真引擎 load + 2tick 空跑不崩」在我跑的 49 测内，绿。

## 三、撤修验红（四步铁律 ②·全部在 %TEMP% 副本·锚点先预告）

| 轮 | 撤修点（副本） | 预告锚点 | 实测 | 判定 |
|---|---|---|---|---|
| 基线 | 无（副本原样） | 副本本身全绿 | 49/49 绿 | 副本是好的 |
| A | wallet 实体走私函数 `smuggled: () => 42` | 「蓝图是纯数据」守卫该红（走私=代码） | **49/49 全绿**；锚点实核 `typeof smuggled === 'function'`、`JSON.stringify` 静默丢弃 | **守卫盲区**（判词①） |
| B | 三拍握手 order 全压平为 0 | 「三拍握手」测试红 + 行为测试红（自锁症状） | **7 红**：三拍握手 + 锄地/播种浇水/睡觉/成熟封顶/收获/背包读数 | 咬得住 |
| C | 删 tileEntity 的 Clickable 行 | 「每格 10 件组件齐全」红，报「tile-r0c0 缺 Clickable」 | **8 红**，报错逐字命中锚点 | 咬得住 |
| D | flow 实体组件名改坏（GameFlow→GameFlowBROKEN） | 「load + 2tick 不崩」红 | 2tick 未红（引擎静默跳过未知组件名·与 A 同款），但 **5 红**（通关/终局冻结/flow 相位/终局出口/宿主 cleanup）——行为层兜住 | 部分命中·行为面强 |
| E | capabilities 塞假能力 `{id:'totally-fake-cap'}` | 守卫只查 typeof id ⇒ 预期 0 红 | **19 红**（世界构建失败）——能力清单是引擎装配输入，**引擎行为兜住**（我预告错了，纪律比预期强） | 咬得住 |

**前后哈希一致**（撤修全程未污染实况）：撤修前后两次 `sha256sum games/game109/*.ts` 均与 §一 数值一致；
副本目录 `%TEMP%/g109-s3-sabotage/` 已删。

## 四、读告警（四步铁律 ④·逐条给结论）

1. **vitest stderr 反复出现的拓扑环 WARN**：`[state-sync, flow]`（闭环组件 State）——引擎既有诊断
   （`src/engine/core/topological-sort.ts` 发出），由世界组件构成触发、非 game109 写法缺陷；只提示
   排序可复现性，走 stderr 不改退出码。上一轮复查已在纯引擎测试中复现过同款，本轮引擎零改动，沿用定性。
2. **vitest stderr 的 ECONNREFUSED ::1/127.0.0.1:3000**（终局出口测试内）：jsdom 里
   `game109.ts:102` 的 `fetch('/games/game109/art/index.json')` 打向 jsdom 默认基址 → 拒绝连接；
   `game109.ts:115` 的 `catch { …回退程序化观感·不炸游戏 }` 优雅兜住（测试 49/49 绿）。
   结论：**预期内噪声，非缺陷**。
3. **我 5173 实测抓到 4×HTTP 500** `http://localhost:5173/api/generate/providers`：根因 = 4000 API
   后端宕（`curl 127.0.0.1:4000` 连接拒绝）→ vite 代理 500 → 启动器「API 状态灯」
   （`src/launcher.tsx:294-297`）报 console error。与 game109 内容无关；但探针零容忍 console error
   ⇒ 判词②的环境时间炸弹。07:51 落盘证据 `consoleErrors: []` 证明门跑时后端在。

## 五、清单逐条结论

| 清单条目 | 结论 | 依据 |
|---|---|---|
| 范围核查（每关必查） | 过 | §一：9 文件 + art index 哈希与 S7 声明基线逐字一致，本回合零改动 |
| manifest 纯 JSON（无代码走私） | **有条件过** | 内容纯数据（逐行读 blueprint.ts：实体全是对象字面量、零游戏层 system、全仓 grep 零 Math.random/innerHTML/eval/new Function，唯一 createElement 在测试脚手架）；编译期免 manifest 是设计（game-pipeline.mjs:54-58 形态判定、:62-64 manifestPath 对 compiled 返 null、:691-698 S3 门分支；handbook testing.md:19 REQ-RENDERCHECK）。**但机器守卫抓不住函数走私（撤修 A 全绿）**——判词① |
| 实体/组件用途与 plan 一致 | 过 | 18 个 capabilities 全部是 `@zerocraft` 引擎实名件（atom-skills/tier1/2/3 import）；plan §4「本次申请为零例外」仍成立；偏离 plan §2 的（effect-apply 门替代 craft-recipe 等）均有 owner 裁决（REQ-G109-001/002/003·requests.md）；无 plan 外私加系统性机制。撤修 C/E 证明组件完备性/能力清单都有机器牙 |
| 落盘门真跑过（load+2tick 证据新鲜） | 过（带环境警示） | 账本 07:51Z exit 0 + 三件产物今日新落；load+2tick 测试在 49 绿内；截图独立解码非空白；点击独立复核与证据逐字一致。⚠ 现态 4000 后端宕 ⇒ 现在重跑门会假红（判词②，非 game109 之过） |
| 组件字段无「填了但没人解释」的死数据 | 过（4 条表级小尾巴照旧） | 组件字段逐条有解释性注释（tile SpriteBinding.resourceId 双模式注释、plantEntity Shape 0 宽注释、kb-* 无 source 注释等）。表/主题级 4 条既存小尾巴（上一轮已记）：`theme.ts:101` BTN_TINT.toolActive（全仓零消费·无死因注释）、`theme.ts:103` READOUT_TINT（注释已解释：读数住 DOM HUD）、`data.ts:137` ToolDef.hint（零消费）、`data.ts:98` CropDef.tintAlt（δ 全轴后撤用但字段+注释残留·data.ts:34-35 已说明撤因）。均无行为影响 |

## 六、我未能查清 / 边界诚实申报

1. **未重跑机器门本身**（本复查不跑 `gate`）——「落盘门真跑过」以账本+产物+独立复读+独立浏览器复核代证。
2. **未在浏览器里走完整玩家链**（till→sow→water→sleep 的画布坐标点击链）——画布路径的世界行为由
   49 条单测覆盖（含撤修 B/C 证明其咬得住），点击门的职责「信号打得穿」已由 DOM 复核证。
3. **撤修 D 中「load+2tick 不崩」未红**的深层原因（引擎静默跳过未知组件名）未追进引擎源码定性——
   这是引擎容错语义问题（与撤修 A 同款），报 Lead 评估，不在本关清单内。
4. 上轮复查第 4 条「六件策划文档 vs 四件」口径问题未复核（S2 范畴，S2 二轮复查已记录）。
