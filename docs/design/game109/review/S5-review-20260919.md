# game109《星露谷式农场 demo》· S5 独立复查报告

- **日期**：2026-09-19（复查当轮）
- **复查主体**：独立复查 agent（S5 · fresh context · 非施工人）——未参与任何 S5 施工步骤
- **判词**：**CONCERNS**（理由与两条命名缺陷见 §5/§6）
- **指纹恒定声明**：复查全程 gameHash 恒为 **`044205f5ad0e21d5`**（开跑前独立重算、收尾再核，均与板上一致；复查有效）
- **唯一开工命令**：`node scripts/game-pipeline.mjs checklist game109 S5`（7 项）——本报告按这 7 项逐项给结论（§3）

---

## 0. 复查口径

- 复查四步铁律（`CLAUDE.md` 2026-08-06）：①独立复跑 ②撤修验红（只许隔离副本）③实证复现 ④读告警——执行记录见 §2。
- 所有结论带 file:line 或实数；凡我未亲跑的项一律标注，不作结论。
- 本仓无 git ⇒ 范围核查用领工声明的基线哈希对照（requests.md:1296-1304）+ S5-alignment.md §4 的 7 件表（:136-144），不是 git diff、不是 mtime。

---

## 1. 范围核查（清单第 1 项）——PASS

### 1.1 声明基线 7 件 · 前后哈希逐件对照（独立重算，非抄施工方表）

| 件 | 开工前（requests.md:1296-1304） | 独立重算现状 | 施工方自陈 | 判定 |
|---|---|---|---|---|
| `games/game109/game109.skeleton.test.ts` | `1b5194b0b880` | `1bce8830ae9a` | `1bce8830ae9a` | 改·声明内（①） |
| `games/game109/game109.ts` | `af27e642df5f` | `672d38c1a922` | `672d38c1a922` | 改·声明内（②a） |
| `games/game109/hud.ts` | `8effe9326191` | `658b7fb49b8b` | `8ec068961517`→`658b7fb49b8b` | 改·声明内（②a+⑤） |
| `games/game109/theme.ts` | `ffe0a78d1994` | `ffe0a78d1994` | 未变 | **未变 ✓** |
| `games/game109/data.ts` | `a9f0a27e0df3` | `a9f0a27e0df3` | 未变 | **未变 ✓** |
| `games/game109/blueprint.ts` | `31fd45b69a9a` | `31fd45b69a9a` | 未变 | **未变 ✓** |
| `scripts/game109-spec-recursion.mjs` | `3323b073eadd` | `3f00d8025302` | `3f00d8025302` | 改·声明内（③） |

- **「theme.ts/data.ts/blueprint.ts 未变」自陈：独立重算逐字节成立**（sha256 前后全同）。
- 4 件改动全部落在边界栏（requests.md:1306-1317）点名名单内；**边界外零改动**。
- 附：撤修验红收尾时对 7 件再次独立重算，与上表逐字相同（隔离副本已删除后）。

### 1.2 gameHash 独立重算

按 `game-pipeline.mjs:82-115` 的口径（4 根 library/`public/games/<slug>`/`games/<slug>`/`docs/design/<slug>`，排除 EVIDENCE_DIRS + pipeline.json + requests.md + capability-gaps.json + EVIDENCE_FILES）独立重算 = **`044205f5ad0e21d5`**，与 pipeline.json 板上值一致。复查期间未再变化。

### 1.3 S2 六指纹锁未动

六输入（`game-pipeline.mjs:118-125`）独立重算：reviewHash = **`1aa02c0219e57d0b`** · gapsHash = **`30d1542ab7ea5415`**，与 pipeline.json `reviews.S2` 绑定值一致；`gdd.md` / `s1-s2-program-handoff.md` / `s1-s2-program-evidence.md` 三份仍为 `<missing>`（未创建）——施工方「明确不碰那六份」属实。

### 1.4 对「mtime 不能作边界证明」的独立判断（复查附加项 ①）

- **站得住的部分**：mtime 只能证明「文件被动过」，不能证明「谁、哪一轮、哪把工具」——本仓一天内 S2–S5 多轮施工 + 工具运行自动写证据文件，单凭 mtime 归因确实会冤。把它当**归因证据**（attribution）用，不成立。**结论：此说作为「不能用 mtime 当证明」是成立的。**
- **夸大的部分**：mtime 作为**检测信号**（detection）仍有价值——「施工窗口内、边界外出现新 mtime」是廉价高敏红旗，应先看再排，而非一律不看。我按此独立扫了 `src/**`、`scripts/**`、`library/**`、`games/**`、`public/**`：施工窗口（本地 22:04–22:41）内的新 mtime **只出现在 3 份声明文件**上；窗口后的代码目录零新增。哈希证明（严格）与 mtime 检测（廉筛）互相印证——边界无越界。

**范围结论：PASS。无越界，无「碰了声明外文件」。**

---

## 2. 复查四步铁律执行记录

### ① 独立复跑（全部当轮亲跑；机器门 + 抽样自证）

| 项 | 当轮结果（2026-09-19 复查当轮） |
|---|---|
| `gate game109 S5`（完整门·audit+ratchet+golden 三段真跑） | **exit 0**（16:46:11Z）· `AUDIT: WARNINGS · RATCHET: PASS · ✓ 当前版本标准照比对过（s5-boot）` · `gameHash 044205f5ad0e21d5` |
| `golden-shot.mjs compare --game game109` ×4 | 4/4 全绿：`ok:true · ratio 0 · diffCount 0/1024000 · maxChannelDiff 0 · stable:true` |
| audit 三臂（`tools/ui-audit.mjs`·UI_AUDIT_CHROMIUM 指向本机 Chrome） | 见 §3.4，全部 exit 0 |
| `scripts/ui-inventory.mjs`（词表 6 项） | **exit 0** · `[ui-inventory] game109 ✓ 词表 6 个动作屏上都够得着` |
| `scripts/render-probe.mjs --game game109` | **exit 0** · `variance 2646.66`（阈值 15）· `consoleErrors 0` · `pageErrors 0` · `gameHash 044205f5ad0e21d5` |
| `font-coverage.probe.mjs` | **exit 0** · 「通关！」三字全由 ZCOOL KuaiLe 画出 · 阳性对照 `！`✓ / 阴性对照 `Ω`✓ 两向准 |
| `npx vite-node scripts/acceptance-run.mjs --game game109` | **9/9 PASS**（357 检查点）· `ACCEPTANCE: PASS · 1 游戏 · 0 家有红` |
| `node scripts/game109-spec-recursion.mjs`（27 条款全量撤修验红） | **exit 0** · `27 条 · 26 条有剧本守着 · 0 条裸奔 · 1 条已知无牙`；实况 5 文件跑前/跑后 sha256 全等（脚本自印 + 我另核） |
| `vitest`（game109 2 文件） | 49/49 全绿（30 skeleton + 19 theme） |
| `npx tsc --noEmit` | **24 条红**，分布 = D-15 逐条吻合：13 `src/skills/tier2/sprite-binding.ts` + 9 其单测 + 1 `src/skills/tier2/knockback.ts` + 1 `games/game-mcfight/r3-b4-public-capabilities.test.ts`；**game109 零条** |
| `scripts/game109-playthrough.mjs`（真 vite + 真 Chromium 玩家路径） | **174/174** · `ok:true` · `consoleErrors 0`（当轮） |

### ② 撤修验红（全部隔离副本；先写锚点预言、后执行、再对账）

**(a) tints 证人（`games/game109/game109.skeleton.test.ts:202-203`）**

- 隔离副本 `.s5-sabotage/`：`blueprint.ts:126` `tints: LIFE_TINTS` → 6 个错值。
- **锚点预言**：唯一红必须红在证人行 `skeleton.test.ts:202:28`（`expect(b['tints']).toEqual(LIFE_TINTS)`），其余照绿。
- **实测**：`1 failed | 29 passed`——失败**恰在 :202:28**，逐字命中锚点。
- **双向对照**：摘掉证人两行（:202-203）再跑 → **30/30 全绿**。⇒ 证人是唯一牙：没有它，6 个错 tints 静默假绿（复现了 S4 复查人当年的实测记录，那条注释是**真话**）。

**(b) hud 换皮回归（`hud.ts:215` `props:{bg:'panel'}`）**

- 隔离副本：`hud.ts:215` → `props: { bare: true }` + 指向副本的 audit 入口（.s5-sabotage/audit-hud-sabotage.audit.ts，同 view 同主题）。
- **锚点预言**：必 4 条硬红，ratio 逐条 = **1.45 / 2.65 / 2.77 / 2.77**（体力/第 N 天/背包/当前工具），exit 1。
- **实测**：4 条硬红、ratio 与节点名**逐条吻合**、exit 1。⇒ /check-ui 对 ②a 修的正是有牙的（4→0 不是侥幸变绿）。

**(c) R-19（递归脚本自带隔离副本机制·`game109-spec-recursion.mjs:13-16`）**

- **锚点预言**：第 16 条「临水门 notWatered」必须被 09 剧本转红（`✓`）。
- **实测**：第 16 条 `✓`，明细逐字点名 `09-water-repeat-no-free-spend.scenario.jsonc`；全套 27 条 26 守 0 裸奔 1 已知无牙（`生长 SelfRule 的自清`，README 有记载）；脚本自印实况 5 文件跑前/跑后 sha256 **全等**。

**收尾**：`.s5-sabotage/` 已删；实况 7 件 sha256 重算与基线逐字相同（见 §1.1）。

### ③ 实证复现（施工方关键数字独立重算）

- 渲染探针 `variance 2646.66`（自证单 §4 记 2646.66）——当轮重跑同数。
- audit-hud 警告 `金币 618/1000` ratio **3.57**（自证单 §4 记 3.57 = D-17）——当轮同数。
- `09` 剧本判别式（09-water-repeat-no-free-spend.scenario.jsonc）：同日第二次浇水后 `{"res":"energy","eq":17}`（:89），对照格真浇 `15→14`（:120）——静态复核判别式成立：`notWatered` 在场 ⇒ 进不了 BUSY ⇒ 扣费拍不施放 ⇒ 停 17；对照格防「浇水永不扣」的坏实现假绿。
- `tsc` 24 条分布与 D-15（requests.md:1025）逐条吻合。
- 递归结论句与 §0.19(g)(1)（requests.md:1546）逐字吻合。

### ④ 读告警（stderr WARN 逐条读，不跳）

1. **gate 的 `AUDIT: WARNINGS`**——唯一警告 = 金币对比度 3.57：低于 AA 正文 4.5、高于硬地板 3.0 ⇒ 非阻断；根因 = D-17 apolloToon `gold` 令牌自身属性（requests.md:1027），**非本轮引入**。
2. **`[topological-sort]` 环告警 `[state-sync, flow]`**（stderr）——引擎既有告警（确定性平局裁决·注册序/tier 序），不影响退出码；S5 未触碰相关文件。既有，非本轮引入。
3. **`tsc` 24 条红** = D-15（S4「δ 全轴」留债，协议接口 `src/engine/protocol/components/render.ts:633-640` 缺 `states/flagId/stride` 三字段；game109 0 条）。
4. 走查 `consoleErrors 0`（当轮）。

**无一条告警指向本轮改动。**

---

## 3. 清单 7 项逐项结论

### 3.1 范围核查 —— **PASS**（§1）

### 3.2 LayoutNode 纪律 —— **PASS**

`hud.ts` 全文复查，S5 改动只有 3 处，全是观感取值：

| 行 | 改动 | 判定 |
|---|---|---|
| `hud.ts:153` | `actionButton` 加 `layout: { fx: [{ kind: 'sheen-hover' }] }`（6 枚键共用） | 观感取值 ✓ |
| `hud.ts:181` | `resultTitle` 加 `font: 'cnround'` | 观感取值 ✓ |
| `hud.ts:215` | 根节点 `props: { bg: 'panel' }`（原 `bare:true`；:202-214 注释记载 4 条硬红 1.45/2.65/2.77/2.77 的来龙） | 观感取值 ✓ |

- `hud.ts` 全文**零 DOM API**、节点树（id/层级/顺序）未增未删——符合 LayoutNode 数据驱动 UI 纪律。
- 引擎依据逐条对到源码：`layoutStyle` 只对显式给出的键发 CSS（`src/ui/components/render.ts:132-134`）⇒ `{fx:[…]}` 几何零变；`sheen-hover` 的 CSS 字面在位（`src/ui/components/server.ts:223-225`）；`font:'cnround'` 不会静默回落（`server.ts:303` 无条件注入 ART_FONT_CSS+ART_FONT_CJK_CSS，字体在仓内 `public/ui-fonts/cjk/cnround.woff2`）。
- `game109.ts:116` `mountUI(..., STARTER_THEME, input)` 为 ②a 唯一点；:140 handlers 表只有「重开」（其余 6 键走 ActionSink 入队）。

### 3.3 audit 棘轮绿 —— **PASS**

- 当轮 gate 判词 **`RATCHET: PASS`**（audit-baseline.json 无 game109 条目 ⇒ 零红旗要求即为绿；`game-skill-audit.mjs:54` 排除测试文件——机制核对无误）。
- 新增 S5 证据（三个 audit 入口）没有引入红旗。

### 3.4 /check-ui 四关 —— **PASS**（当轮三臂 + golden 比对）

| 臂 | 当轮结果 |
|---|---|
| `audit-hud.audit.ts`（--w 740 --h 140） | **exit 0** · 硬失败 0 · 警告 1（金币 ratio 3.57=D-17）· 华丽度 **12**（skin:6 fx:6）· house 主题：**是** |
| `audit-hud-shell.audit.ts`（同参） | **exit 0** · 0/0/0 · 华丽度 6（fx:6）· house 主题：**否**（壳屏不经换皮主题·正证主题只在游戏屏生效） |
| `audit-result.audit.ts`（--w 740 --h 200） | **exit 0** · 0/0/0 · 华丽度 1（skin:1）· house 主题：是 |
| golden 像素比对（第 4 关·s5-boot blessed） | gate 内 ✓（16:46:11Z）+ standalone ×4 ratio 0 |

与自证单 §4（S5-alignment.md:121）逐字吻合。华丽度 6→12（fx:6 正是⑤那六枚键）属实。

### 3.5 交互可发现 —— **PASS**

- `ui-inventory` 词表 6 项（pick-reap/pick-sow/pick-till/pick-water/sell/sleep）**屏上都够得着**，exit 0。
- 走查 174/174：真 vite + 真 Chromium + 只点真按钮，终局出口必点（新局能接着打）。

### 3.6 只换皮不动布局 —— **PASS**

- `theme.ts` 几何常量（FIELD_W/FIELD_H/HUD_H/BTN_*/Z）**一个字节没动**（哈希 `ffe0a78d1994` 前后同）。
- `hud.ts` 节点树结构未动（§3.2）；几何键（x/y/width/height/flex/padding/margin）未动——`layoutStyle` 的几何只读节点自身 layout（`render.ts:132+`），不读 theme。
- **最强实证**：golden 像素比对 `ratio 0 · diffCount 0/1024000 · maxChannelDiff 0 · stable:true`——与 blessed 基准**逐像素一致**。「换皮不动布局」不是自陈，是像素级事实。
- 施工方在 S5-alignment.md:72 主动留的诚实口子（`t.fontUi` 随主题变 ⇒ flex 内可能几像素位移）**在 golden ratio 0 面前不成立**——实际零位移。诚实口子没发生。

### 3.7 自证对齐单抽样重走（≥3 条）+ 好玩三问 —— **PASS**

抽样重走远超 3 条（§2① 全套：探针/字体/验收/递归/走查/golden/audit 全过）。好玩三问（S5-alignment.md §5:148-163）已读：答得诚实、具体、有证据图背书——三处磨损（D-9 读数竖排、D-13 双控件、D-19 被拒零反馈）全部如实列为本作问题而非粉饰，节奏检查（无 >1s 干等窗口）的判据（玩家动作推天、走查 174 步无等待墙钟）成立。**采纳其结论**。

---

## 4. 11 条 ⚠ 处置落账核验（owner 裁决行 F 是否真落地）

| ⚠#（S5-alignment §2） | 自陈去向 | requests.md 落账位置 | 核验 |
|---|---|---|---|
| #1 Label.format 空操作 | 报裁 #3 记 D-14 | :1024 D-14 行（owner 裁 B） | ✓ |
| #2 Panel.skin 未挂 | 报裁 #3 记 D-14 | :1024 | ✓ |
| #3 网格容器未做 | 报裁 #3 记 D-14 | :1024 | ✓ |
| #4 Particles 未做 | 报裁 #3 记 D-14 | :1024 | ✓ |
| #5 起手包两屏未用 | owner 已裁·记债 | :1356（裁决行 E ②b）· :1360 | ✓ |
| #6 D-9 读数折行 | 报裁 #1 | :1020 D-9 行（owner 裁 B·归 ②b/D-13 一揽子） | ✓ |
| #7 D-13 两套控件并存 | 报裁 #1 同归 D-13 | 裁决行 F #1（:1568）归 ②b/D-13 | **✗ 债表无 D-13 行**（见 F-1） |
| #8 D-19 被拒零反馈 | 归 S4 交接 | :1029 D-19 行（owner 裁 B·归 S4）· :1572 | ✓ |
| #9 D-15 tsc 24 红 | 报裁 #2 | :1025 D-15 行（owner 裁 B·不捆绑） | ✓ |
| #10 §0.10 双构造 | 报裁 #4 保债 | :1014 D-3 · :1018 D-8（owner 裁 B） | ✓ |
| #11 胜负特效/飘字 | 报裁 #3 记 D-14 | :1024 | ✓ |

**10/11 有据落账；唯一缺口 = D-13 债表行缺失（F-1）。** §0.20 表头「已回填债表归属列」（requests.md:1566）对 D-13 不成立。

---

## 5. 发现

### F-1（文档缺陷·非游戏字节）：D-13 债表缺行——被引用 6+ 次、表里一行都没有

债表（requests.md:1010-1030）行号连续覆盖 D-1…D-12、D-14…D-20，**唯独没有 D-13**（:1023 是 D-12，:1024 直接是 D-14）。而 D-13 被引用至少 6 处：§0.18:1360「记为 D-13（见下方债表）」· §0.19(f):1527「D-13（§0.18 已记·②b）不变」· §0.20:1568/#1569/#1570（三处把 D-9、D-14 都「归 D-13 一揽子」）· S5-alignment.md:92/:93（§2 #6/#7）。

- **性质**：D-13 是「②b 换屏结构未落地 + 两套控件并存 + 真 HUD 合并动作」——demo 最显眼的一笔观感欠账，却是债表里**唯一没有行**的那笔。§0.20 表头「已回填债表归属列」对 D-13 不成立。
- **影响**：不影响游戏字节、不影响机器门；影响账本可信度（下一关/S6 接棒者按债表盘点时 D-13 会漏）。
- **修法（一行）**：在 :1023（D-12）与 :1024（D-14）之间补一行 `| D-13 | ②b 换屏结构未落地（自建 HUD/结算面板·无主菜单屏·两套控件并存） | owner 裁 B（§0.18+§0.20）：记债 · 归「真 HUD」/S6 一揽子（D-9/D-14 同归） |`。

### F-2（工具/环境缺陷·非本轮引入）：golden 门 30s goto 零余量——负载下门会假红

- **现象（亲历两次）**：我最早两次 `gate game109 S5`（16:31:39Z、16:33Z）均 exit 1，判词 `✗ 标准照漂移`·reason `装载失败 · page.goto: Timeout 30000ms exceeded`。
- **root cause 定位（实测链）**：
  1. `golden-shot.mjs:205` 硬编码 `page.goto(url, { waitUntil: 'load', timeout: 30000 })`，**零重试、零余量**；
  2. 新起 vite 后**首次**装载的时长重尾分布：空闲时 12.4s（当轮实测·259 请求全完·canvas/data-action 都挂载），机器有并发 I/O（当时我有后台全仓 find 扫描压盘）时 >30s；
  3. 最慢请求全是引擎模块（`src/skills/tier2/*` 逐个 ~3.8s、`src/assembly/*` ~3.8s），**与 S5 改动零关系**（范围核查已证 src/** 零改动）；
  4. 对照：standalone `golden-shot.mjs compare` 当轮 4/4 全绿（ratio 0）；空闲机复跑完整 gate **exit 0**（16:46:11Z）——同一份游戏字节，门红/绿随机器负载翻转。
- **结论**：不是像素漂移（ratio 恒 0），是**装载超时在 30s 上被误判成漂移**。S5 无责，但 owner 需要知道：机器门现役判词里「✗ 标准照漂移」这一红**可能不指向任何代码改动**，只指向负载。施工方在 :1596 写的「该 gate exit 0 可复现」**只在空闲机上成立**。
- **建议（工具侧，不阻塞本关）**：`golden-shot.mjs:205` 的 30s → 45–60s，或装载失败时重试一次，或起服后先发一个 warm-up 请求再 goto。另注：复查落账后施工方计划重跑 gate 再请 owner 重签（:1596-1597）——建议在空闲机跑，并知道有这条时序脆弱性。

### F-3（正面结论）：施工方自陈诚实度抽查全部属实

三条诚实交代（S5-alignment.md:14-18）、哈希链（§4）、判词（§4 各门）、以及「③ 撤修验红前先实证转红再转标记」的顺序纪律——全部经独立复现核实，无一处吹胀。本报告 F-1/F-2 之外未发现任何游戏字节缺陷。

---

## 6. 判词与建议

**CONCERNS。**

- **为什么不是 FAIL**：无游戏字节缺陷；全部实质门当轮绿（gate exit 0 含 golden 比对、audit 三臂 0 硬红、验收 9/9、递归 27/26/0/1、走查 174/174、golden ratio 0）；范围零越界；撤修验红 3 锚点全中证明施工方的证人/门禁牙齿是真的。打回无法指向「改哪一行游戏代码」。
- **为什么不是 PASS**：两条命名缺陷——F-1（D-13 债表缺行，一行可修，账本可信度受损）与 F-2（golden 门时序零余量，负载下会假红，owner 重跑门时可能被吓一跳）。两条都不动游戏字节、都不需要重跑全套，但都应修掉再走下一关。
- **给 owner 的行动清单**：
  1. 采纳 F-1 修法（requests.md:1023/1024 之间补 D-13 行）；
  2. 工具侧处置 F-2（golden-shot.mjs:205 超时/重试/warm-up 三选一）；
  3. 复查落账后由施工方重跑 `gate game109 S5`（其计划内·:1596-1597）→ owner 人门签核。

---

## 附 A：当轮命令与关键实数快照（供复核）

```
gate game109 S5            → exit 0 · AUDIT: WARNINGS · RATCHET: PASS · ✓ 当前版本标准照比对过（s5-boot）· 16:46:11Z · gameHash 044205f5ad0e21d5
golden compare ×4          → ratio 0 · diffCount 0/1024000 · maxChannelDiff 0 · stable:true
audit-hud                 → exit 0 · 0 硬红 · 1 警告(金币 3.57) · 华丽度 12 · house:是
audit-hud-shell           → exit 0 · 0/0/0 · 华丽度 6 · house:否
audit-result              → exit 0 · 0/0/0 · 华丽度 1 · house:是
ui-inventory              → exit 0 · 词表 6 项都够得着
render-probe              → exit 0 · variance 2646.66 · consoleErrors 0 · gameHash 044205f5ad0e21d5
font-coverage probe       → exit 0 · 通关！×3 全 ZCOOL KuaiLe · 阳性！/阴性Ω 两向准
acceptance-run            → 9/9 PASS · 357 检查点
spec-recursion            → 27 条 · 26 守 · 0 裸奔 · 1 已知无牙 · 实况 sha256 前后全等
vitest game109 2 文件      → 49/49
tsc --noEmit              → 24 条（D-15 分布）· game109 0
playthrough               → 174/174 · ok:true · consoleErrors 0
撤修验红(a)                → 1 failed 恰在 skeleton.test.ts:202:28 / 摘证人 30/30 绿
撤修验红(b)                → 4 硬红 1.45/2.65/2.77/2.77 逐条命中 / exit 1
撤修验红(c)                → 第 16 条 ✓ 被 09 抓住
范围                       → 7 件基线哈希逐件独立重算 · 3 未变 4 声明内 · gameHash 独立重算吻合 · S2 六指纹未动
```
