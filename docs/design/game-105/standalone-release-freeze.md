# game-105 独立可玩版冻结决定

> GD-105 · 2026-09-02 · owner 决策：先完成游戏，不以 Storyteller/真实模型集成为当前阻塞项。

## 当前发布目标

本版交付“玩家与本地 AI 可完整游玩的一局心动叠叠塔”：54 块真物理、可见 AI 抽拉、实时倒塌、战利品、玩家互动卡输入、AI 卡牌与惩罚卡、本局心动值、重开、可读 UI 和完整质量验收。

AI 的文字统一使用本地模板。现有 Host adapter、App manifest 与结果投影可以保留为**未启用的未来接缝**，但不得要求启动 Host、配置模型、访问网络、调用 `dialogue` 或产生外部回传才能玩完一局。

## 延后范围

`G105-E6-REAL-DIALOGUE-HOST-WITNESS` 进入 Host Integration M2：真实模型文本、Storyteller 启动、结果回传、赛后续聊与记忆候选展示均不参与本版 S3--S8 验收。确定性 SDK witness 作为未来兼容性证据保留，不能作为本版功能宣传。

## 验收优先级

1. 重新完成本版 S2 复查与 owner 签核，明确真实模型缺口为延期而不是已交付。
2. S3 以 `s3-standalone-release.md` 验证游戏能在无 Host 条件下启动，AI 仍使用共享物理执行器，保留接缝不影响本地运行。
3. S4 以 `s4-local-ai-m1.md` 与 `s4-experience-extension.md` 验证完整本地玩法闭环和 AI 模板卡牌/惩罚卡。
4. S5、S6、S7 重做视觉、可读性、窄屏、遮挡与可点击性验收。
5. S8 运行全量类型检查、测试、构建和独立复查后形成独立可玩版发布候选。

真实模型 M2 只能在独立立项后恢复；不得以“未来会接入”为由降低本版玩法或 UI 质量门槛。
