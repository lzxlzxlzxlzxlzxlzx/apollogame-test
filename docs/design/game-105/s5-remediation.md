# game-105《心动叠叠塔》S5 复查失败整改单

> GD-105 · 2026-08-29 · 前次 S5 独立复查为 `FAIL`。本单是重新施工前的硬前置，不得以重跑原 S5 gate 替代。

## 1. 阻塞结论

1. `golden/s4-frozen.png` 已被 S5 候选 capture 覆盖。台账虽保留首张的 SHA-256 `be518f...`，但不保留原始图片字节；当前同名图片不能作为 S4 冻结照。
2. `src/ui/components/render.ts` 的无条件 `pointer-events:auto` 是全仓交互语义改动，不属于 game-105 的 S5 视觉换皮。它必须移出本次交付或在独立平台变更中修复并完成全仓回归。

在两项关闭前，禁止 S5 gate、bless、独立复查或 owner 签核。

## 2. 不可变基准恢复

必须从 S4 签核时的可验证源码/构建快照重建基准，目标哈希为 `e182544eefb77401`。不能用当前 S5 候选、不能用当前 `s4-frozen.png`，也不能把相似的 S4 试玩截图伪装为 golden 原件。

- 若仓库存在该签核版本的提交、归档或可复现构建：在隔离 worktree 以固定 `1280x800` 重新捕获，命名为 `s4-structure-e182544eefb77401`。
- 若找不到该版本的可复现来源：登记“原始标准照证据丢失”，停止 S5；由 owner 决定重新打开 S4 并重建、复查和签核基准。不得以新的 S5 画面补签旧 S4。
- 基准文件必须写入不可覆盖的存档路径，并记录来源提交/构建标识、图像 SHA-256、视口、捕获时间和操作者。后续 capture 必须生成新候选文件，不能覆盖 blessed 基准文件。

程序若需修改 `golden-shot` 工具以支持不可变归档，这是独立的流程工具改动：应附工具回归测试，且不得混入 game-105 UI 源码变更。

## 3. S5 候选与结构比对

S4 基准与 S5 候选必须是不同证据对象：

- `s4-structure-e182544eefb77401`：只读的 S4 冻结依据。
- `s5-skin-candidate-<当前哈希>`：本轮可替换候选；仅 owner 批准视觉结果后才可 bless 为 S5 自身的后续回归基准。
- S5 不得 bless、capture 或改写任何 `s4-structure-*` 条目。

像素图仅能证明画面变化，不能单独证明“换皮未改布局”。因此必须追加一个 S4 结构快照/断言，至少锁定所有 HUD、操作区、互动卡区、余波提示区、收尾卡区的 `id`、可见状态、父子顺序、锚点和尺寸。S5 当前结构与 S4 冻结结构必须完全相同；颜色、字体、纹样和贴图不进入该结构快照。

## 4. 交互修复边界

- 删除或回退本轮 `render.ts` 的无条件 `pointer-events:auto` 改动。
- 若 game-105 的具体 UI 有点击穿透问题，只能在 `games/game-105/` 的具体 LayoutNode、容器层或组件配置修复；不得改变共享渲染器的默认交互语义。
- 任何确实需要共享组件修复的提案，必须另开平台任务，提供全仓快照/交互回归，不得附带在本 S5 交付中。

## 5. 重提 S5 的验收顺序

1. 恢复不可变 S4 基准，或由 owner 正式裁决重开 S4；记录证据来源。
2. 回退/隔离全仓 `pointer-events` 改动，运行全仓 Vitest，恢复零新增失败；game-g 和 game211 的 UI 快照必须通过。
3. 仅在 game-105 进行 S5 允许的颜色、字体、纹样、贴图和非语义动效调整。
4. 生成独立 S5 候选，运行 S4 结构快照比对、`/check-ui`、Chrome 桌面/窄屏走查、游戏定向回归与全仓 Vitest。
5. 补写 `self-check/S5-alignment.md`：逐项链接 S4 不可变基准、S5 候选、结构对照、全仓回归和截图。
6. 运行 S5 gate，交独立复查；复查 `PASS` 后才交 owner 签核。

## 6. 非目标

- 不因本次整改改动 S4 玩法、AI、物理、卡牌状态、输入或布局。
- 不把 S5 的候选图当作 S4 基准重新 bless。
- 不在本次 S5 解决其他游戏的 UI 交互问题。
