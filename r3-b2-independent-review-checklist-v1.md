# R3-B2 独立复查清单 v1.0

本清单只在程序提交 B2 冻结副本后使用。复查者不得修改共享施工目录，不得以目录存在、配置快照或单元 mock 替代生产链路证据。

## 1. 冻结完整性

- 冻结副本包含源码、内容配置、测试、证据、矩阵和运行命令。
- 八个 B2 单位在冻结目录均有生产模板、决策候选、表现配置与点名测试映射。
- 矩阵的 36/84 仅在以下所有检查通过后填写；复查报告单独标注程序通过与独立复查通过。

## 2. 公共能力核对

### Cone

- 协议字段为 `radius`，无运行时 `range` 双语义。
- 真实 cone 对 circle 接触覆盖内侧、外弧边界、角边界、角度外与半径外。
- 引导方向成功开始时锁定；连续段数、间隔、硬控/死亡取消与区域清理可从 Tick 轨迹复核。

### Volley

- 一次施放产生一个 `VolleyPlan`；每枚弹有不同实体、`castId`、`shotIndex`、来源与来源 Tag 快照。
- 烈焰人按 0/2/4 Tick 发射三枚；铜羽泽鹗同拍发射两枚。
- 弹体发射位置读取来源在该 Tick 的当前 Transform；方向来自施放开始时快照。
- 来源硬控/死亡、原目标死亡/失效取消未发射子弹；已发射弹仍按独立实体清理。
- 烈焰人散布由实例私有 seeded PRNG 生成；同 seed 复现、不同实例隔离。

## 3. 单位身份核对

按 [B2 身份验收矩阵](../r3-b2-identity-acceptance-matrix-v1.md) 逐行核对八单位，不接受“通用身份测试全绿”替代标志性行为。

- 三个状态单位：直接伤害与 DOT 回执区分、刷新不叠层、免疫正确、来源快照保留。
- 凋零骷髅：前摇承诺、凋零和取消边界。
- 烈焰人：逐弹轨迹、三发间隔、单次命中清理和 burn 刷新。
- 喷火甲虫/寒冬狼：同一 cone 模板、不同状态；四段几何和中断清理。
- 铜羽泽鹗：双羽独立、对地/对空、护甲与韧性均置零的直接伤害计算，且无敌/友军过滤仍有效。

## 4. 撤修验红

独立副本中至少执行以下 6 组带锚点撤修，再恢复并复跑：

1. 将 cone 半径外目标改为命中，边界测试必须失败。
2. 删除 cone 方向锁定，横移目标场景必须失败。
3. 把 Volley 间隔改为每拍，烈焰人发射 Tick 测试必须失败。
4. 合并两枚铜羽为同一实体，独立实体/shotIndex 测试必须失败。
5. 删除来源死亡取消，未发射余弹测试必须失败。
6. 移除铜羽 `armorPiercing`，护甲/韧性计算测试必须失败。

## 5. 最终门

复跑 B2、B1、R3 身份与受影响共享回归；类型检查、构建和完整组合审计必须通过。S3/S4 如因源变更而过期，报告中标为待复核，不能自动继承通过。

任何撤修不验红、生产链路缺失、Tick 证据不完整或共享回归失败，结论均为退回修改；不得因为 36/84 已写入矩阵而签核。
