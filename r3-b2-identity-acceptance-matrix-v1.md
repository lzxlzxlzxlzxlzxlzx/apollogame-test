# R3-B2 八单位身份验收矩阵 v1.0

本矩阵把 B2 从“目录可编译”推进到“可提交独立复查”的逐单位最低证据。所有场景必须使用生产 `assembleCombatWorld`、生产 Caster、真实移动、Overlap、Trigger、Hitbox、伤害、状态和清理链。任何直接写入命中、伤害或状态的 fixture 均不计入身份通过。

## 全单位共同门

每个单位都必须证明：

1. 本单位的生产决策能选择其合法主技能；
2. 两个错时实例至少完成三轮可复现行为，来源、目标、CD、随机状态和生成实体不串扰；
3. 来源在启动前硬控或死亡时不产生未发生攻击；成功启动 CD 不返还；
4. 来源死亡后，未发射/未发生阶段清理，已发射弹或已发生 DOT 按自身合同结束；
5. 所有临时区域、投射物、计划实体和状态都在合同规定的时点清理；
6. 测试记录真实伤害回执、来源实体、来源 Tag、目标及 Tick。

## 单位场景

| 单位 | 主场景 | 必须量化断言 |
|---|---|---|
| `stray` | 标准减速箭 | 三轮命中；每弹位置至少两帧不同；每弹一次命中并销毁；目标仅有一个 slow 实例，第二次命中刷新至 100 Tick。 |
| `iceandfire_if_cockatrice` | 高频凋零弹 | 0.4 秒 CD 下三轮命中；直接伤害和 wither 周期回执分开；wither 为 80 Tick、每 20 Tick 3 点真伤；来源死亡后已有 DOT 完成。 |
| `cataclysm_the_watcher` | 燃烧弹与免疫 | 三轮命中和 burn 刷新；正常目标获得 200 Tick burn；火焰免疫目标无 burn 且无周期伤害。 |
| `wither_skeleton` | 近战凋零 | 目标捕获后前摇，即使目标拉远仍兑现；实际近战命中写入 wither；硬控或来源死亡取消未释放的一击。 |
| `blaze` | 三连火球 | 每轮生成 3 个同一 `castId`、不同 `shotIndex` 的弹体，索引 0/1/2，发射间隔 2 Tick；每弹独立飞行、最多一次命中与清理；三弹命中只刷新一个 burn；相同 seed 轨迹一致。 |
| `twilightforest_fire_beetle` | 锁向喷火扇形 | 64/24、60°、4 段、10 Tick 间隔；内侧、外弧边界、角边界命中，半径/角度外不命中；每段每目标一次；开始后目标横移不改方向；中断清理余段。 |
| `twilightforest_winter_wolf` | 锁向冰雾扇形 | 使用与喷火甲虫同一生产模板；验证相同几何及段数，但附加 slow，并验证同种 slow 刷新而不叠层。 |
| `iceandfire_stymphalianbird` | 双羽与穿甲 | 同一 `castId` 下同拍生成两个不同弹体，索引 0/1；两弹独立飞行/命中/销毁；有护甲、韧性目标按二者为 0 得到直接伤害，但无敌、友军、非法目标仍不命中；覆盖对地和对空。 |

## 跨机制场景

在八个单位场景之外保留以下公共交叉验证：

- 目标同时有 burn、wither、slow 时，三个效果共存；再次命中同种效果只刷新该效果。
- 齐射第二枚发射前来源遭硬控或死亡：未发射余弹取消，已发射弹继续。
- 引导第二段前来源死亡：已发生段保留结果，未发生段和区域清理。
- 目标在 volley 后续子弹前死亡：尚未发射余弹取消；已经发射的弹保留其方向快照，并可与后续遇到的其他合法目标真实碰撞。

## 通过记录要求

每一行通过前，矩阵必须记录：生产模板 ID、测试文件、运行命令、通过数、关键 Tick 轨迹和证据文档路径。八行全部完成，再更新 `full-restoration-matrix.json` 为 `passed-program` 并把 R3 计数从 28/84 提升到 36/84；独立复查前不得称为正式完成。

