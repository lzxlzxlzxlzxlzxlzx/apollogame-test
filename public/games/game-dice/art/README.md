# d6 prototype asset

`d6-prototype.stl` is a user-supplied local visual prototype for this development checkout.
It is not a production or distributable asset. Physics and result mapping remain implemented by
the generic Cannon d6 collider; the STL only replaces the rendered appearance during local tests.
